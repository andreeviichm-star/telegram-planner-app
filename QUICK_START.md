# 🚀 Быстрый старт

## За 5 минут до запуска в Telegram

### 1. Установка зависимостей
```bash
cd /Users/andreeviich/telegram-planner-app
npm run install:all
```

### 2. Установка ngrok
```bash
brew install ngrok
# или скачайте с https://ngrok.com/download
```

### 3. Создание бота в Telegram
1. Откройте [@BotFather](https://t.me/botfather)
2. `/newbot` → следуйте инструкциям
3. `/newapp` → создайте Mini App (URL пока не указывайте)

### 4. Запуск приложения

**Автоматически:**
```bash
./start-dev.sh
```

**Или вручную:**
```bash
# Терминал 1 - Backend
cd backend && npm run dev

# Терминал 2 - Frontend  
cd frontend && npm run dev

# Терминал 3 - ngrok
ngrok http 3000
```

### 5. Подключение к Telegram
1. Откройте http://localhost:4040 (ngrok dashboard)
2. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
3. В BotFather: `/myapps` → выберите приложение → Edit → Web App URL
4. Вставьте URL от ngrok

### 6. Готово! 🎉
Откройте вашего бота в Telegram и нажмите "Open App"

---

## Полезные команды BotFather

- `/mybots` - список ботов
- `/myapps` - список Mini Apps
- `/setmenubutton` - кнопка в меню бота

## Решение проблем

**Приложение не открывается:**
- Проверьте, что URL использует HTTPS
- Убедитесь, что ngrok запущен
- Проверьте консоль браузера (DevTools)

**API не работает:**
- Убедитесь, что backend запущен на порту 3001
- Проверьте `frontend/.env` - должен быть `VITE_API_URL=http://localhost:3001/api`

