# 🔧 Исправление пути публикации в Netlify

## Проблема

Netlify ищет `frontend/frontend/dist`, но должно быть `frontend/dist`.

Это происходит потому, что:
- `base = "frontend"` уже указывает на папку frontend
- `publish = "frontend/dist"` добавляет еще один `frontend/` к пути

---

## Решение

Я исправил `netlify.toml` - убрал `frontend/` из пути publish.

Теперь:
- `base = "frontend"` - Netlify работает из папки frontend
- `publish = "dist"` - публикует из `frontend/dist` (относительно base)

---

## Что нужно сделать

### Шаг 1: Загрузите изменения на GitHub

```bash
cd /Users/andreeviich/telegram-planner-app

# Добавьте измененный файл
git add frontend/netlify.toml

# Создайте коммит
git commit -m "Fix Netlify publish path"

# Загрузите на GitHub
git push
```

### Шаг 2: Передеплойте на Netlify

Netlify автоматически передеплоит после push (2-3 минуты).

Или вручную:
1. Откройте [netlify.com](https://netlify.com)
2. Откройте проект
3. **Deploys** → **Trigger deploy** → **Deploy site**

---

## Альтернатива: Настройка через веб-интерфейс

Если файл не помогает, настройте через веб-интерфейс:

1. Netlify → **Site settings** → **Build & deploy** → **Build settings**
2. **Base directory**: `frontend`
3. **Build command**: `npm run build`
4. **Publish directory**: `dist` (не `frontend/dist`!)
5. Сохраните
6. Передеплойте

---

## Проверка

После передеплоя:
- ✅ Сборка должна пройти успешно
- ✅ Деплой должен завершиться
- ✅ Приложение должно работать

---

## Готово! 🎉

После загрузки изменений деплой должен пройти успешно!

