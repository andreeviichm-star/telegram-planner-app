# 🚂 Деплой на Railway - Подробная инструкция

## Где найти Root Directory в Railway

### Способ 1: После создания проекта

1. После выбора репозитория и создания проекта
2. Нажмите на **ваш проект** (не на сервис, а на сам проект)
3. Откройте вкладку **Settings**
4. Прокрутите вниз до секции **Build & Deploy**
5. Найдите **Root Directory** или **Working Directory**
6. Введите: `backend`
7. Сохраните

### Способ 2: В настройках сервиса

1. После создания проекта Railway автоматически создаст сервис
2. Нажмите на **сервис** (Service)
3. Откройте вкладку **Settings**
4. Найдите **Root Directory** или **Source**
5. Введите: `backend`
6. Сохраните

### Способ 3: Если не видите Root Directory

Railway может автоматически определить структуру. Попробуйте:

1. Создайте файл `railway.toml` в корне проекта (уже создан)
2. Или создайте файл `Procfile` в папке `backend`:

```bash
cd /Users/andreeviich/telegram-planner-app/backend
echo "web: npm start" > Procfile
```

3. Или используйте файл `package.json` в backend - Railway сам определит

---

## Пошаговая инструкция деплоя на Railway

### Шаг 1: Создайте проект

1. Откройте [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `telegram-planner-app`
6. Нажмите **"Deploy Now"**

### Шаг 2: Настройте Root Directory

**Вариант A: Через Settings проекта**

1. После создания проекта нажмите на **ваш проект** (название проекта сверху)
2. Откройте вкладку **Settings** (слева в меню)
3. Прокрутите до **Build Settings**
4. Найдите поле **Root Directory**
5. Введите: `backend`
6. Нажмите **Save**

**Вариант B: Через Settings сервиса**

1. Нажмите на **сервис** (Service) внутри проекта
2. Откройте **Settings**
3. Найдите **Source** или **Root Directory**
4. Введите: `backend`

**Вариант C: Через Variables (если Root Directory не видно)**

1. Откройте **Variables** (вкладка в проекте)
2. Добавьте переменную:
   - Key: `RAILWAY_SERVICE_ROOT`
   - Value: `backend`

### Шаг 3: Добавьте переменные окружения

1. В проекте откройте вкладку **Variables**
2. Нажмите **"New Variable"**
3. Добавьте переменные:

```
TELEGRAM_BOT_TOKEN = 8203311307:AAGjMsjPyy3Dfaj11gSoRt_hd4J11cxTJoE
PORT = 3001
NODE_ENV = production
```

4. Нажмите **Save** для каждой переменной

### Шаг 4: Дождитесь деплоя

1. Railway автоматически начнет деплой
2. Вы увидите логи в реальном времени
3. Дождитесь сообщения "Deployment successful"

### Шаг 5: Получите URL

1. После успешного деплоя откройте вкладку **Settings**
2. Найдите секцию **Domains**
3. Или нажмите на **"Generate Domain"**
4. Скопируйте URL (например: `https://your-app.railway.app`)

---

## Альтернатива: Если Root Directory не работает

### Вариант 1: Использовать railway.toml

Файл `railway.toml` уже создан в корне проекта. Railway должен его автоматически подхватить.

### Вариант 2: Создать отдельный репозиторий для backend

Если ничего не помогает:

1. Создайте новую папку только с backend кодом
2. Загрузите на GitHub как отдельный репозиторий
3. Деплойте этот репозиторий на Railway

### Вариант 3: Использовать Render.com

Если Railway не подходит, используйте Render:

1. Откройте [render.com](https://render.com)
2. New → Web Service
3. Connect GitHub repo
4. Настройки:
   - **Root Directory**: `backend`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. Environment Variables:
   - `TELEGRAM_BOT_TOKEN=8203311307:AAGjMsjPyy3Dfaj11gSoRt_hd4J11cxTJoE`
   - `PORT=3001`

---

## Проверка деплоя

После деплоя проверьте:

1. Откройте URL вашего сервиса
2. Добавьте `/health` в конце URL
3. Должно показать: `{"status":"ok"}`

Например: `https://your-app.railway.app/health`

---

## Структура файлов для Railway

Railway ищет:
- `package.json` в корне (или в Root Directory)
- `railway.toml` в корне проекта
- `Procfile` в корне (или в Root Directory)

У вас уже есть:
- ✅ `backend/package.json`
- ✅ `railway.toml` в корне
- ✅ `backend/railway.toml`

---

## Если все еще не работает

1. Проверьте логи деплоя в Railway
2. Убедитесь, что `backend/package.json` существует
3. Проверьте, что в `backend/package.json` есть скрипт `"start": "node server.js"`
4. Попробуйте создать `Procfile` в папке `backend`:

```bash
cd /Users/andreeviich/telegram-planner-app/backend
echo "web: npm start" > Procfile
```

Затем закоммитьте и запушьте:
```bash
cd /Users/andreeviich/telegram-planner-app
git add backend/Procfile
git commit -m "Add Procfile for Railway"
git push
```

Railway автоматически передеплоит с новым файлом.

