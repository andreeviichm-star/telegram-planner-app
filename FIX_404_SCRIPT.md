# 🔧 Исправление 404 для скрипта

## Проблема

```
GET https://andreeviichm-star.github.io/telegram-planner-app/assets/index-CEzktReb.js 
net::ERR_ABORTED 404 (Not Found)
```

Файл не найден, хотя должен быть создан при сборке.

## Возможные причины:

1. **Неправильный путь в index.html** - Vite не заменил `/src/main.tsx` на правильный путь
2. **Проблема с base path** - пути не учитывают `/telegram-planner-app/`
3. **Файл не создался при сборке** - проблема в процессе сборки

## Что исправлено:

1. ✅ Изменен путь в `index.html` с `/src/main.tsx` на `./src/main.tsx` (относительный)
2. ✅ Добавлена более подробная проверка build output в workflow

## Что проверить после деплоя:

1. **Проверьте логи workflow:**
   - Откройте: https://github.com/andreeviichm-star/telegram-planner-app/actions
   - Найдите последний workflow run
   - Проверьте шаг "Verify build output"
   - Должны увидеть содержимое `index.html` и список файлов в `assets/`

2. **Проверьте, что файлы созданы:**
   - В логах должно быть: `assets/index-[hash].js`
   - Если файлов нет - проблема в сборке

3. **Проверьте index.html после сборки:**
   - В логах должно быть правильное содержимое `<script>` тега
   - Должно быть что-то вроде: `<script type="module" src="/telegram-planner-app/assets/index-[hash].js">`

## Если проблема останется:

Возможно, нужно использовать другой подход - создать `index.html` через Vite plugin или использовать абсолютный путь с base.

## Загрузка:

```bash
cd /Users/andreeviich/telegram-planner-app
git add .
git commit -m "Fix script path in index.html, add build verification"
git push
```

После деплоя проверьте логи workflow - там будет видно, что именно создается при сборке!

