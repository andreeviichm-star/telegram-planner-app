# 🔧 Исправление белого экрана в Telegram

## Анализ логов

По логам видно:
- ✅ Страница загружается (`onPageFinished`)
- ✅ Ресурсы запрашиваются (`assets/index.js`, `assets/index.css`)
- ✅ Telegram WebApp инициализируется (`web_app_ready`, `web_app_expand`)
- ⚠️ Но приложение может показывать белый экран

## Возможные причины

### 1. Backend недоступен
Приложение пытается загрузить данные с backend, но если backend недоступен, может показывать белый экран или ошибки.

### 2. JavaScript ошибки
Логи Telegram не показывают JavaScript ошибки. Возможно, есть ошибки в коде, которые блокируют выполнение.

### 3. Переменная окружения не установлена
Если `VITE_API_URL` не установлена, приложение будет пытаться подключиться к `http://localhost:3001/api`, что не сработает.

---

## Решение

### Шаг 1: Проверьте backend

1. Откройте backend URL в браузере:
   - `https://your-backend.railway.app/health`
2. Должно показать: `{"status":"ok"}`
3. Если не работает - проблема с backend

### Шаг 2: Проверьте секрет VITE_API_URL

1. GitHub → **Settings** → **Secrets and variables** → **Actions**
2. Убедитесь, что секрет `VITE_API_URL` добавлен
3. Значение должно быть правильным (например: `https://your-backend.railway.app/api`)
4. **Важно:** URL должен заканчиваться на `/api` (не просто `/`)

### Шаг 3: Добавьте обработку ошибок

Я добавлю обработку ошибок, чтобы приложение работало даже если backend недоступен.

### Шаг 4: Проверьте в браузере

1. Откройте URL в браузере на телефоне:
   - `https://andreeviichm-star.github.io/telegram-planner-app/`
2. Откройте консоль (если доступна)
3. Какие ошибки?

---

## Что проверить

1. ✅ **Backend доступен?** (`/health` работает?)
2. ✅ **Секрет `VITE_API_URL` добавлен?** (в GitHub Settings → Secrets)
3. ✅ **URL правильный?** (должен заканчиваться на `/api`)
4. ✅ **Работает ли сайт в браузере на телефоне?**

---

## Сообщите

1. **Работает ли backend?** (`/health` показывает `{"status":"ok"}`?)
2. **Добавлен ли секрет `VITE_API_URL`?** (и какое значение?)
3. **Что показывает сайт в браузере на телефоне?** (работает/белый экран/ошибка)

Это поможет точно определить проблему!

