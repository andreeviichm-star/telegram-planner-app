# 🔧 Исправление: git pull перед push

## Проблема

Удаленный репозиторий содержит изменения, которых нет локально (вероятно, workflow был обновлен через веб-интерфейс).

## Решение

Выполните команды в терминале:

```bash
cd /Users/andreeviich/telegram-planner-app
git pull --rebase
```

Если возникнут конфликты:
```bash
# Если есть конфликты, разрешите их, затем:
git add .
git rebase --continue
```

Если хотите отменить rebase:
```bash
git rebase --abort
```

После успешного pull:
```bash
git add .
git commit -m "Fix script path in index.html"
git push
```

## Альтернатива: если pull не нужен

Если вы уверены, что хотите перезаписать удаленные изменения:

```bash
git pull --rebase
# Если всё ок, то:
git push
```

Или если точно знаете, что нужно перезаписать:
```bash
git push --force
```

⚠️ **Внимание:** `--force` перезапишет удаленные изменения. Используйте только если уверены!

## Рекомендация:

**Используйте `git pull --rebase`** - это безопаснее и сохранит изменения из удаленного репозитория.

