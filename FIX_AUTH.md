# 🔐 Решение проблемы аутентификации GitHub

## Проблема: `fatal: Authentication failed`

GitHub больше не принимает пароли напрямую. Нужно использовать **Personal Access Token**.

---

## Решение 1: Создать Personal Access Token (рекомендуется)

### Шаг 1: Создайте токен на GitHub

1. Откройте https://github.com
2. Нажмите на ваш аватар (правый верхний угол)
3. Выберите **Settings**
4. В левом меню найдите **Developer settings** (внизу)
5. Нажмите **Personal access tokens** → **Tokens (classic)**
6. Нажмите **Generate new token** → **Generate new token (classic)**
7. Заполните форму:
   - **Note**: `Telegram Planner App` (любое название)
   - **Expiration**: выберите срок (например, 90 days или No expiration)
   - **Select scopes**: поставьте галочку на **`repo`** (это даст доступ к репозиториям)
8. Нажмите **Generate token** (внизу страницы)
9. **ВАЖНО:** Скопируйте токен сразу! Он показывается только один раз!
   Токен выглядит так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 2: Используйте токен вместо пароля

Вернитесь в Terminal и выполните:

```bash
cd /Users/andreeviich/telegram-planner-app
git push -u origin main
```

Когда попросит:
- **Username**: введите ваш GitHub username (`andreeviichm-star`)
- **Password**: вставьте токен (который вы скопировали, начинается с `ghp_`)

Готово! Код должен загрузиться.

---

## Решение 2: Сохранить токен в Git (чтобы не вводить каждый раз)

После успешного push, вы можете сохранить токен:

### macOS:

```bash
# Сохранить токен в Keychain
git config --global credential.helper osxkeychain
```

Теперь при следующем push токен сохранится в Keychain и не нужно будет вводить его снова.

---

## Решение 3: Использовать SSH вместо HTTPS

Если хотите использовать SSH (не требует токенов):

### Шаг 1: Проверьте, есть ли SSH ключ

```bash
ls -al ~/.ssh
```

Если видите файлы `id_rsa` и `id_rsa.pub` - ключ есть. Переходите к шагу 3.

### Шаг 2: Создайте SSH ключ (если нет)

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

Нажмите Enter на все вопросы (можно оставить пароль пустым).

### Шаг 3: Скопируйте публичный ключ

```bash
cat ~/.ssh/id_ed25519.pub
```

Скопируйте весь вывод (начинается с `ssh-ed25519`).

### Шаг 4: Добавьте ключ на GitHub

1. GitHub → Settings → SSH and GPG keys
2. New SSH key
3. Title: `MacBook` (любое название)
4. Key: вставьте скопированный ключ
5. Add SSH key

### Шаг 5: Измените remote на SSH

```bash
cd /Users/andreeviich/telegram-planner-app
git remote set-url origin git@github.com:andreeviichm-star/telegram-planner-app.git
git push -u origin main
```

Теперь не нужно вводить пароль или токен!

---

## Решение 4: Использовать GitHub Desktop (самый простой)

Если не хотите возиться с токенами:

1. Скачайте: https://desktop.github.com
2. Установите и войдите в аккаунт
3. File → Add Local Repository
4. Выберите: `/Users/andreeviich/telegram-planner-app`
5. Нажмите "Publish repository"
6. Выберите имя и нажмите "Publish"

GitHub Desktop сам разберется с аутентификацией!

---

## Быстрая команда (если уже создали токен)

Если вы уже создали токен, просто выполните:

```bash
cd /Users/andreeviich/telegram-planner-app
git push -u origin main
```

И введите:
- Username: `andreeviichm-star`
- Password: ваш токен (начинается с `ghp_`)

---

## Проверка

После успешного push откройте:
https://github.com/andreeviichm-star/telegram-planner-app

Вы должны увидеть все файлы проекта!

