# 🚂 Railway - Где найти Root Directory

## Проблема: Не вижу Root Directory

В Railway эта настройка может быть в разных местах в зависимости от версии интерфейса.

---

## Где искать Root Directory:

### Вариант 1: В настройках проекта (Settings)

1. После создания проекта нажмите на **название проекта** (сверху, не на сервис)
2. Откройте вкладку **Settings** (слева в меню)
3. Прокрутите до секции **"Build & Deploy"** или **"Build Settings"**
4. Найдите поле **"Root Directory"** или **"Working Directory"**
5. Введите: `backend`
6. Нажмите **Save**

### Вариант 2: В настройках сервиса

1. Нажмите на **сервис** (Service) внутри проекта
2. Откройте вкладку **Settings**
3. Найдите **"Source"** или **"Root Directory"**
4. Введите: `backend`

### Вариант 3: При создании проекта

1. После выбора репозитория, **перед** нажатием "Deploy Now"
2. Посмотрите внизу или справа - может быть кнопка **"Configure"** или **"Settings"**
3. Там может быть поле Root Directory

---

## Если не нашли - используйте Procfile (проще!)

Я уже создал файл `Procfile` в папке `backend`. Просто загрузите его на GitHub:

```bash
cd /Users/andreeviich/telegram-planner-app
git add backend/Procfile
git commit -m "Add Procfile for Railway"
git push
```

Railway автоматически определит, что нужно использовать папку `backend`.

---

## Альтернатива: Используйте Render.com

Если Railway не подходит, Render проще:

1. Откройте [render.com](https://render.com)
2. **New** → **Web Service**
3. Connect ваш GitHub репозиторий
4. Настройки:
   - **Root Directory**: `backend` ← **Здесь точно есть!**
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. Environment Variables:
   - `TELEGRAM_BOT_TOKEN=8203311307:AAGjMsjPyy3Dfaj11gSoRt_hd4J11cxTJoE`
   - `PORT=3001`
6. **Create Web Service**

Render проще в настройке!

---

## Визуальная подсказка для Railway

Ищите в интерфейсе Railway:

```
Project Settings
├── General
├── Build & Deploy  ← Здесь!
│   └── Root Directory: [backend]
├── Variables
└── ...
```

Или:

```
Service Settings
├── General
├── Source  ← Или здесь!
│   └── Root Directory: [backend]
└── ...
```

---

## Быстрый способ (без Root Directory)

1. Создайте файл `railway.json` в корне (уже есть)
2. Railway автоматически использует его
3. Или используйте `Procfile` в backend (уже создан)

Просто загрузите изменения на GitHub и Railway передеплоит автоматически!

