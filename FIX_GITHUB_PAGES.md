# 🔧 Исправление: GitHub Pages показывает README вместо приложения

## Проблема
GitHub Pages показывает текст (README.md) вместо приложения.

## Причина
GitHub Pages настроен на деплой из ветки, а не из GitHub Actions.

---

## Решение

### Шаг 1: Проверьте настройки GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. **Settings** → **Pages**
3. Проверьте **Source**:
   - ❌ Если выбрано: **"Deploy from a branch"** - это неправильно!
   - ✅ Должно быть: **"GitHub Actions"**

4. Если неправильно:
   - Выберите **"GitHub Actions"**
   - Сохраните

### Шаг 2: Проверьте, запустился ли workflow

1. Репозиторий → **Actions** (вверху)
2. Проверьте, есть ли запуски workflow "Deploy to GitHub Pages"
3. Если нет - значит workflow не запустился

### Шаг 3: Запустите workflow вручную (если нужно)

1. Репозиторий → **Actions**
2. Выберите workflow "Deploy to GitHub Pages"
3. Нажмите **"Run workflow"** (справа)
4. Выберите ветку: **main**
5. Нажмите **"Run workflow"**

### Шаг 4: Дождитесь завершения деплоя

1. В **Actions** дождитесь завершения workflow
2. Должен быть зеленый статус ✅
3. После этого сайт должен обновиться

---

## Если workflow не запускается

### Проверьте файл workflow

1. Убедитесь, что файл `.github/workflows/deploy.yml` существует
2. Проверьте его содержимое - должно быть правильным YAML

### Проверьте секреты

1. **Settings** → **Secrets and variables** → **Actions**
2. Убедитесь, что секрет `VITE_API_URL` добавлен

### Проверьте логи

1. В **Actions** откройте последний запуск
2. Посмотрите логи - какие ошибки?

---

## После исправления

1. ✅ GitHub Pages настроен на "GitHub Actions"
2. ✅ Workflow успешно выполнился
3. ✅ Сайт показывает приложение, а не README

URL должен работать: `https://andreeviichm-star.github.io/telegram-planner-app/`

---

## Если все еще не работает

Проверьте:
1. Правильно ли настроен base path в `vite.config.ts` (должен быть `/telegram-planner-app/`)
2. Правильно ли собирается проект (проверьте логи workflow)
3. Правильно ли указан `publish_dir` в workflow (должен быть `./frontend/dist`)

Сообщите, что видите в настройках Pages и Actions!

