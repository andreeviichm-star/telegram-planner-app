# 🔧 Исправление ошибки 404 на GitHub Pages

## Проблема
```
Failed to load resource: the server responded with a status of 404
```

## Причины
1. Workflow не запустился или не выполнился успешно
2. Неправильный base path
3. Ресурсы не загружаются из-за неправильных путей

---

## Решение

### Шаг 1: Проверьте, запустился ли workflow

1. Репозиторий → **Actions**
2. Проверьте, есть ли успешные запуски workflow "Deploy to GitHub Pages"
3. Если нет - запустите вручную:
   - Actions → Deploy to GitHub Pages → Run workflow

### Шаг 2: Проверьте логи workflow

1. Откройте последний запуск workflow
2. Проверьте логи:
   - Успешно ли установились зависимости?
   - Успешно ли собрался проект?
   - Успешно ли задеплоился на GitHub Pages?

### Шаг 3: Проверьте настройки GitHub Pages

1. **Settings** → **Pages**
2. Убедитесь, что **Source** = **"GitHub Actions"**
3. Если неправильно - измените и сохраните

### Шаг 4: Проверьте base path

Base path должен быть `/telegram-planner-app/` для GitHub Pages.

Проверьте файл `frontend/vite.config.ts` - должно быть:
```typescript
base: process.env.VITE_BASE_PATH || (process.env.NODE_ENV === 'production' ? '/telegram-planner-app/' : '/'),
```

### Шаг 5: Перезапустите workflow

1. **Actions** → **Deploy to GitHub Pages**
2. Нажмите **"Run workflow"**
3. Выберите ветку: **main**
4. Нажмите **"Run workflow"**
5. Дождитесь завершения

---

## Проверка после исправления

1. Откройте сайт: `https://andreeviichm-star.github.io/telegram-planner-app/`
2. Откройте консоль (F12)
3. Проверьте, нет ли ошибок 404
4. Если есть - посмотрите, какие именно ресурсы не загружаются

---

## Если все еще не работает

### Проверьте секреты

1. **Settings** → **Secrets and variables** → **Actions**
2. Убедитесь, что секрет `VITE_API_URL` добавлен
3. Если нет - добавьте его

### Проверьте структуру файлов

После деплоя в папке `gh-pages` должны быть:
- `index.html`
- `assets/` (с JS и CSS файлами)

### Очистите кэш браузера

1. Нажмите Ctrl+Shift+R (или Cmd+Shift+R на Mac)
2. Или откройте в режиме инкогнито

---

## Быстрое решение

Если ничего не помогает, попробуйте:

1. **Удалите workflow** через веб-интерфейс
2. **Создайте заново** с правильным содержимым
3. **Запустите вручную**
4. **Проверьте результат**

---

## Что проверить

1. ✅ Workflow успешно выполнился
2. ✅ GitHub Pages настроен на "GitHub Actions"
3. ✅ Base path правильный (`/telegram-planner-app/`)
4. ✅ Секрет `VITE_API_URL` добавлен
5. ✅ Сайт открывается без ошибок 404

Сообщите, что видите в логах workflow и в консоли браузера!

