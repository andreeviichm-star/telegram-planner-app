# 📝 Команды для синхронизации с GitHub

## Выполните эти команды в терминале:

```bash
# Перейдите в папку проекта
cd /Users/andreeviich/telegram-planner-app

# Получите изменения с GitHub
git pull --rebase

# Если все прошло успешно, загрузите ваши изменения
git push
```

---

## Если появились конфликты:

```bash
# После git pull --rebase, если есть конфликты:
# 1. Откройте файлы с конфликтами
# 2. Разрешите конфликты вручную
# 3. Затем:
git add .
git rebase --continue
git push
```

---

## Если pull прошел успешно:

Просто выполните:
```bash
git push
```

---

## Готово!

После успешного push:
- Изменения будут на GitHub
- Cloudflare Pages автоматически задеплоит новую версию

