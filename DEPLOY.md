# 🚀 Деплой Mini App в облако (без ngrok)

Этот гайд покажет, как задеплоить приложение на бесплатные облачные платформы.

## Архитектура деплоя

- **Frontend** → Vercel (бесплатно, автоматический деплой)
- **Backend** → Railway или Render (бесплатный тариф)

---

## Вариант 1: Vercel + Railway (рекомендуется)

### Шаг 1: Подготовка репозитория

1. Создайте репозиторий на GitHub:
   ```bash
   cd /Users/andreeviich/telegram-planner-app
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. Создайте репозиторий на GitHub.com и запушьте:
   ```bash
   git remote add origin https://github.com/ваш-username/telegram-planner-app.git
   git branch -M main
   git push -u origin main
   ```

### Шаг 2: Деплой Backend на Railway

1. Зарегистрируйтесь на [Railway.app](https://railway.app) (через GitHub)

2. Создайте новый проект:
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. Настройте проект:
   - **Root Directory**: `backend`
   - **Build Command**: (оставьте пустым, Railway сам определит)
   - **Start Command**: `npm start`

4. Добавьте переменные окружения:
   - Нажмите на проект → Settings → Variables
   - Добавьте:
     ```
     PORT=3001
     TELEGRAM_BOT_TOKEN=8203311307:AAGjMsjPyy3Dfaj11gSoRt_hd4J11cxTJoE
     NODE_ENV=production
     ```

5. Дождитесь деплоя и скопируйте URL:
   - Railway автоматически создаст URL (например: `https://your-app.railway.app`)
   - Скопируйте этот URL - это ваш backend API

### Шаг 3: Деплой Frontend на Vercel

1. Зарегистрируйтесь на [Vercel.com](https://vercel.com) (через GitHub)

2. Импортируйте проект:
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - **Root Directory**: `frontend`
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

3. Добавьте переменные окружения:
   - В настройках проекта → Environment Variables
   - Добавьте:
     ```
     VITE_API_URL=https://your-app.railway.app/api
     ```
     (замените на ваш URL от Railway)

4. Деплой:
   - Нажмите "Deploy"
   - Дождитесь завершения
   - Скопируйте URL (например: `https://your-app.vercel.app`)

### Шаг 4: Подключение к Telegram

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/myapps`
3. Выберите ваше приложение
4. **Edit** → **Web App URL**
5. Вставьте URL от Vercel: `https://your-app.vercel.app`
6. Подтвердите

### Готово! 🎉

Теперь Mini App доступен 24/7 без вашего компьютера!

---

## Вариант 2: Vercel + Render (альтернатива)

### Backend на Render

1. Зарегистрируйтесь на [Render.com](https://render.com) (через GitHub)

2. Создайте новый Web Service:
   - Connect ваш GitHub репозиторий
   - **Root Directory**: `backend`
   - **Environment**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`

3. Добавьте переменные окружения:
   ```
   PORT=3001
   TELEGRAM_BOT_TOKEN=8203311307:AAGjMsjPyy3Dfaj11gSoRt_hd4J11cxTJoE
   NODE_ENV=production
   ```

4. Создайте сервис и скопируйте URL (например: `https://your-app.onrender.com`)

5. Frontend на Vercel (как в Варианте 1, но используйте URL от Render):
   ```
   VITE_API_URL=https://your-app.onrender.com/api
   ```

---

## Вариант 3: Все на Vercel (только Frontend, Backend отдельно)

Если хотите использовать только Vercel:

### Frontend на Vercel
Следуйте инструкциям из Варианта 1, Шаг 3

### Backend на отдельном сервисе
- Railway (рекомендуется)
- Render
- Fly.io
- Heroku (платный)

---

## Настройка CORS для продакшена

Убедитесь, что в `backend/server.js` CORS настроен правильно:

```javascript
app.use(cors({
  origin: [
    'https://your-app.vercel.app',
    'https://your-app--*.vercel.app' // для preview деплоев
  ],
  credentials: true
}))
```

Или для всех доменов (менее безопасно, но проще для начала):
```javascript
app.use(cors())
```

---

## Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите "Open App"
3. Проверьте:
   - ✅ Приложение открывается
   - ✅ Задачи создаются
   - ✅ Календарь работает
   - ✅ API запросы проходят

---

## Обновление кода

После каждого push в GitHub:
- **Vercel** деплоит автоматически
- **Railway** деплоит автоматически (если включен auto-deploy)

---

## Мониторинг

### Vercel
- Dashboard показывает логи и метрики
- Бесплатный тариф: 100GB bandwidth/месяц

### Railway
- Dashboard показывает логи
- Бесплатный тариф: $5 кредитов/месяц (достаточно для небольшого приложения)

---

## Решение проблем

### Backend не отвечает
- Проверьте логи в Railway/Render dashboard
- Убедитесь, что переменные окружения установлены
- Проверьте, что порт правильный

### Frontend не подключается к API
- Проверьте `VITE_API_URL` в Vercel
- Убедитесь, что backend URL правильный
- Проверьте CORS настройки

### Mini App не открывается
- Убедитесь, что URL в BotFather правильный
- Проверьте, что URL использует HTTPS
- Проверьте консоль браузера в Telegram

---

## Стоимость

**Бесплатные тарифы:**
- Vercel: бесплатно (достаточно для большинства проектов)
- Railway: $5 кредитов/месяц (обычно хватает)
- Render: бесплатно (с ограничениями)

**Итого: $0-5/месяц** для небольшого приложения

---

## Безопасность

⚠️ **Важно:**
- Не коммитьте `.env` файлы в Git
- Используйте переменные окружения в панелях платформ
- Токен бота уже в `.gitignore`

---

## Быстрый чеклист

- [ ] Репозиторий на GitHub
- [ ] Backend задеплоен (Railway/Render)
- [ ] Frontend задеплоен (Vercel)
- [ ] Переменные окружения настроены
- [ ] Web App URL обновлен в BotFather
- [ ] Приложение работает в Telegram

