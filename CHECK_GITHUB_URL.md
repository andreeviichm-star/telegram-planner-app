# 🔍 Проверка URL GitHub Pages

## Важно: Проверьте ваш URL

Ошибка 404 обычно возникает из-за неправильного base path.

---

## Шаг 1: Проверьте URL вашего сайта

1. Откройте ваш репозиторий на GitHub
2. Settings → Pages
3. Посмотрите **URL** (например):
   - `https://andreeviichm-star.github.io/telegram-planner-app/`
   - ИЛИ `https://andreeviichm-star.github.io/`

---

## Шаг 2: Определите base path

### Если URL: `https://username.github.io/telegram-planner-app/`
→ Base path должен быть: `/telegram-planner-app/`

### Если URL: `https://username.github.io/`
→ Base path должен быть: `/`

---

## Шаг 3: Обновите переменную окружения

В GitHub Actions workflow добавьте переменную:

```yaml
- name: Build
  run: |
    cd frontend
    npm run build
  env:
    VITE_API_URL: ${{ secrets.VITE_API_URL }}
    VITE_BASE_PATH: /telegram-planner-app/  # Или / если корневой домен
```

---

## Шаг 4: Пересоберите проект

После обновления workflow:
1. Сделайте любой коммит в папке `frontend/`
2. GitHub Actions автоматически пересоберет проект
3. Проверьте работу сайта

---

## Быстрое решение

Если не хотите менять workflow, можно временно изменить base path в `vite.config.ts`:

```typescript
base: '/telegram-planner-app/',  // Или '/' если корневой домен
```

Затем загрузите изменения и пересоберите.

---

## Проверка

После исправления:
1. Откройте сайт в браузере
2. Откройте консоль (F12)
3. Проверьте, нет ли ошибок 404
4. Если есть - проверьте пути к ресурсам

Сообщите, какой URL у вас - помогу настроить правильно!

