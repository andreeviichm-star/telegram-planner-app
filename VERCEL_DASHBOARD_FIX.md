# 🔧 Настройка заголовков через Vercel Dashboard

Проблема с MIME type требует настройки через веб-интерфейс Vercel, так как vercel.json не всегда применяется правильно.

---

## Решение: Настройка через Vercel Dashboard

### Шаг 1: Откройте настройки проекта

1. Откройте [vercel.com](https://vercel.com)
2. Войдите в аккаунт
3. Откройте проект `telegram-planner-app`
4. Откройте вкладку **Settings**

### Шаг 2: Найдите раздел Headers

В Settings найдите один из разделов:
- **Headers** (если есть)
- **Security** → **Headers**
- **General** → прокрутите вниз до **Headers**

Если раздела Headers нет, переходите к Шагу 3.

### Шаг 3: Добавьте заголовки

Нажмите **"Add Header"** или **"Add Rule"** и добавьте:

**Правило 1:**
- **Source Path** или **Path**: `/assets/*.js`
- **Header Name**: `Content-Type`
- **Header Value**: `application/javascript; charset=utf-8`
- Сохраните

**Правило 2:**
- **Source Path** или **Path**: `/*.js`
- **Header Name**: `Content-Type`
- **Header Value**: `application/javascript; charset=utf-8`
- Сохраните

### Шаг 4: Передеплойте

1. Откройте вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите на **три точки** (⋮)
4. Выберите **"Redeploy"**
5. Дождитесь завершения

---

## Альтернатива: Если раздела Headers нет

Если в Settings нет раздела Headers, попробуйте:

### Вариант 1: Использовать Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите
vercel login

# Перейдите в папку frontend
cd /Users/andreeviich/telegram-planner-app/frontend

# Создайте файл vercel.json с правильными настройками
# (уже создан, но можно проверить)

# Деплой
vercel --prod
```

### Вариант 2: Создать новый проект

Иногда проще создать проект заново:

1. В Vercel удалите текущий проект (Settings → Danger Zone → Delete)
2. Создайте новый проект из того же репозитория
3. При создании:
   - Root Directory: `frontend`
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Добавьте переменную `VITE_API_URL`
5. После деплоя настройте Headers (если доступно)

---

## Альтернатива 2: Изменить способ сборки

Попробуйте изменить настройки сборки в Vercel:

1. Settings → General → Build & Development Settings
2. **Build Command**: `npm run build`
3. **Output Directory**: `dist`
4. **Install Command**: `npm install`
5. Сохраните и передеплойте

---

## Альтернатива 3: Использовать другой хостинг

Если Vercel продолжает создавать проблемы, можно использовать:

1. **Netlify** - часто лучше работает с Vite
2. **Cloudflare Pages** - бесплатно и быстро
3. **GitHub Pages** - простой вариант

Но сначала попробуйте настроить через Dashboard.

---

## Проверка после настройки

1. Откройте приложение в браузере
2. F12 → Network
3. Найдите файл `main-xxx.js` или `index-xxx.js`
4. Откройте его
5. Посмотрите на **Response Headers**
6. Должно быть: `Content-Type: application/javascript` или `text/javascript`

Если там `application/octet-stream` - заголовки не применяются.

---

## Если ничего не помогает

Попробуйте связаться с поддержкой Vercel или использовать другой хостинг. Но обычно настройка через Dashboard решает проблему.

