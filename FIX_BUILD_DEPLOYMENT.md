# 🔧 Исправление: deploy.yml не появляется в Build and deployment

## Проблема
В настройках GitHub Pages (Settings → Pages → Build and deployment) не появляется опция выбрать workflow "deploy.yml".

## Причина
Workflow не имеет правильных permissions для создания деплоймента GitHub Pages.

## Решение

### Шаг 1: Обновите workflow

Я обновил workflow, добавив необходимые permissions:
- `pages: write` - для записи в GitHub Pages
- `id-token: write` - для аутентификации

### Шаг 2: Обновите файл на GitHub

1. Откройте `.github/workflows/deploy.yml` на GitHub
2. Нажмите на карандаш (✏️)
3. Замените секцию `permissions` на:
```yaml
permissions:
  contents: write
  pages: write
  id-token: write
```
4. Сохраните (Commit changes)

### Шаг 3: Перезапустите workflow

1. **Actions** → **Deploy to GitHub Pages**
2. Нажмите **"Run workflow"**
3. Дождитесь завершения

### Шаг 4: Проверьте настройки Pages

1. **Settings** → **Pages**
2. В разделе **"Build and deployment"** должна появиться опция:
   - **Source**: **GitHub Actions**
   - **Workflow**: **Deploy to GitHub Pages** (deploy.yml)
3. Выберите workflow и сохраните

---

## Альтернатива: Использовать деплой из ветки

Если GitHub Actions все еще не работает, можно использовать деплой из ветки:

1. **Settings** → **Pages**
2. **Source**: выберите **"Deploy from a branch"**
3. **Branch**: выберите **gh-pages** (если она есть)
4. **Folder**: оставьте **/ (root)**
5. Сохраните

Но это временное решение. Лучше использовать GitHub Actions.

---

## Проверка

После обновления workflow и перезапуска:

1. ✅ **Workflow выполнился** успешно?
2. ✅ **Settings → Pages** - появилась опция "GitHub Actions"?
3. ✅ **Workflow выбран** в настройках Pages?
4. ✅ **Сайт работает**?

---

## Если все еще не работает

Попробуйте:

1. **Удалить и пересоздать** настройки Pages
2. **Перезапустить workflow** несколько раз
3. **Подождать 10-15 минут** после деплоя

Сообщите, появилась ли опция "GitHub Actions" после обновления workflow!

