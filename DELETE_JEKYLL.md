# 🗑️ Как удалить Jekyll workflows

## Способ 1: Через веб-интерфейс GitHub (рекомендуется)

### Шаг 1: Откройте файлы

1. Зайдите на GitHub: `https://github.com/andreeviichm-star/telegram-planner-app`
2. Перейдите в папку `.github/workflows/`
3. Вы увидите файлы:
   - `static.yml`
   - `jekyll-gh-pages.yml`
   - (и возможно `deploy.yml` - его НЕ удаляйте!)

### Шаг 2: Удалите файлы

Для каждого файла (`static.yml` и `jekyll-gh-pages.yml`):

1. **Нажмите на файл** (например, `static.yml`)
2. **Нажмите на иконку корзины** (🗑️) в правом верхнем углу
   - Или нажмите на карандаш (✏️) и затем "Delete file"
3. **Внизу страницы:**
   - Введите сообщение коммита: `Remove Jekyll workflow`
   - Нажмите **"Commit changes"** (зеленая кнопка)
4. **Повторите для второго файла** (`jekyll-gh-pages.yml`)

---

## Способ 2: Через терминал (альтернатива)

Если хотите удалить через git:

```bash
cd /Users/andreeviich/telegram-planner-app

# Синхронизируйте с GitHub
git pull --rebase

# Удалите файлы
git rm .github/workflows/static.yml
git rm .github/workflows/jekyll-gh-pages.yml

# Создайте коммит
git commit -m "Remove Jekyll workflows"

# Загрузите на GitHub
git push
```

---

## После удаления

1. ✅ Jekyll workflows удалены
2. ✅ Остался только наш workflow `deploy.yml`
3. ✅ GitHub Pages будет использовать наш workflow

---

## Проверка

После удаления:

1. Откройте `.github/workflows/` на GitHub
2. Должен остаться только файл `deploy.yml`
3. В **Actions** не должно быть Jekyll workflows

---

## Важно

⚠️ **НЕ удаляйте файл `deploy.yml`!** Это наш workflow для деплоя приложения.

Удаляйте только:
- ❌ `static.yml`
- ❌ `jekyll-gh-pages.yml`

Оставляйте:
- ✅ `deploy.yml`

---

## Готово!

После удаления Jekyll workflows:
1. Запустите наш workflow вручную (Actions → Deploy to GitHub Pages → Run workflow)
2. Или сделайте любой коммит в папке `frontend/` - workflow запустится автоматически

