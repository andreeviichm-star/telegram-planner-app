# 🚀 Как запустить сервер на Railway

## Проблема: Сервер офлайн

Если сервер офлайн в Railway, нужно проверить настройки и запустить его.

## Шаг 1: Проверьте настройки проекта

### 1.1. Root Directory
1. Откройте ваш проект в Railway
2. Перейдите в настройки сервиса (Settings)
3. Найдите **"Root Directory"** или **"Source"**
4. Установите: `backend`
   - Это нужно, если ваш бэкенд в папке `backend/`

### 1.2. Start Command
1. В настройках сервиса найдите **"Start Command"**
2. Установите: `npm start`
   - Или: `node server.js`

### 1.3. Build Command (опционально)
1. Если есть **"Build Command"**, оставьте пустым или установите: `npm install`

## Шаг 2: Проверьте переменные окружения

1. В Railway откройте вкладку **"Variables"**
2. Убедитесь, что есть переменная `PORT` (Railway устанавливает автоматически)
3. Другие переменные не обязательны для базового запуска

## Шаг 3: Запустите сервис

### Вариант 1: Через интерфейс Railway
1. Откройте ваш сервис в Railway
2. Найдите кнопку **"Deploy"** или **"Redeploy"**
3. Нажмите её
4. Дождитесь завершения деплоя

### Вариант 2: Через GitHub (если подключен)
1. Сделайте commit и push:
   ```bash
   cd /Users/andreeviich/telegram-planner-app
   git add .
   git commit -m "Trigger Railway deploy"
   git push
   ```
2. Railway автоматически задеплоит изменения

## Шаг 4: Проверьте логи

1. После деплоя откройте вкладку **"Logs"** или **"Deployments"**
2. Посмотрите последние логи
3. Должны увидеть:
   ```
   Server running on port 3001
   ```
   или
   ```
   Server running on port <PORT>
   ```

## Шаг 5: Проверьте статус

1. В Railway откройте ваш сервис
2. Статус должен быть: **"Active"** или **"Running"**
3. Если статус **"Stopped"** - нажмите **"Start"** или **"Deploy"**

## Если всё равно не работает:

### Проверьте структуру проекта:
```
telegram-planner-app/
  ├── backend/
  │   ├── server.js
  │   ├── package.json
  │   └── ...
  └── frontend/
      └── ...
```

### Убедитесь, что в Railway:
- ✅ Root Directory = `backend`
- ✅ Start Command = `npm start`
- ✅ Сервис подключен к репозиторию или загружен код

## Быстрая проверка:

1. **Root Directory**: `backend` ✅
2. **Start Command**: `npm start` ✅
3. **Status**: Active/Running ✅
4. **Logs**: "Server running on port..." ✅

Если всё это настроено правильно, сервер должен запуститься!

