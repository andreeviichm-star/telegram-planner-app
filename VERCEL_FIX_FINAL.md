# 🔧 Финальное исправление ошибки Vercel

## Проблема

Vercel пытается выполнить `cd frontend && npm install`, но не может найти папку `frontend` потому что команды выполняются из корня проекта.

---

## Решение: Настройка через веб-интерфейс Vercel

### Вариант 1: Настроить Root Directory (самый простой)

1. Откройте [vercel.com](https://vercel.com)
2. Откройте ваш проект `telegram-planner-app`
3. Откройте вкладку **Settings**
4. Найдите секцию **General**
5. Найдите поле **Root Directory**
6. Нажмите **"Edit"**
7. Введите: `frontend`
8. Нажмите **"Save"**

**ВАЖНО:** После этого все команды будут выполняться из папки `frontend` автоматически!

### Шаг 2: Удалите старые настройки команд

1. В том же разделе Settings → General
2. Найдите **Build & Development Settings**
3. Убедитесь, что:
   - **Build Command**: `npm run build` (или пусто - будет автоматически)
   - **Output Directory**: `dist` (или пусто - будет автоматически)
   - **Install Command**: `npm install` (или пусто - будет автоматически)

Если там указано `cd frontend && ...` - удалите `cd frontend &&` часть!

### Шаг 3: Передеплойте

1. Откройте вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите на **три точки** (⋮)
4. Выберите **"Redeploy"**

---

## Вариант 2: Удалить проект и создать заново

Если настройки не сохраняются:

1. В Vercel откройте Settings проекта
2. Прокрутите вниз до **"Danger Zone"**
3. Нажмите **"Delete Project"**
4. Создайте проект заново:
   - **Add New Project**
   - Выберите репозиторий
   - **Before deploying, configure your Project:**
     - **Root Directory**: нажмите **"Edit"** → введите `frontend`
     - **Framework Preset**: `Vite`
     - Остальное оставьте по умолчанию
   - **Environment Variables**:
     - `VITE_API_URL` = `https://your-backend-url.railway.app/api`
   - **Deploy**

---

## Вариант 3: Использовать Vercel CLI из папки frontend

Если веб-интерфейс не работает:

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите
vercel login

# Перейдите в папку frontend
cd /Users/andreeviich/telegram-planner-app/frontend

# Деплой (Vercel автоматически определит настройки)
vercel

# Следуйте инструкциям:
# - Set up and deploy? Y
# - Which scope? ваш аккаунт
# - Link to existing project? N
# - Project name? telegram-planner-app
# - Directory? ./ (текущая папка)
# - Override settings? N

# Добавьте переменную окружения
vercel env add VITE_API_URL production
# Введите: https://your-backend-url.railway.app/api

# Деплой в продакшен
vercel --prod
```

---

## Проверка после исправления

После настройки Root Directory и передеплоя:

1. Проверьте логи - должно быть:
   ```
   Running "install" command: `npm install`...
   ```
   (БЕЗ `cd frontend &&`!)

2. Должно быть:
   ```
   Build Completed
   ```

3. Откройте URL приложения
4. Должна открыться страница

---

## Почему это происходит?

Vercel по умолчанию выполняет команды из корня репозитория. Если проект находится в подпапке (например, `frontend`), нужно указать **Root Directory** в настройках проекта.

После указания Root Directory:
- Все команды выполняются из указанной папки
- Не нужно писать `cd frontend &&` в командах
- Vercel автоматически находит `package.json` в папке `frontend`

---

## Быстрый чеклист

- [ ] Удалили `vercel.json` из корня (уже сделано)
- [ ] Настроили Root Directory: `frontend` в Vercel Settings
- [ ] Убрали `cd frontend &&` из команд (если были)
- [ ] Добавили переменную `VITE_API_URL`
- [ ] Передеплоили проект
- [ ] Проверили логи - команды выполняются из `frontend`

---

## Если все еще не работает

Проверьте логи деплоя в Vercel:
1. Откройте последний деплой
2. Откройте **Build Logs**
3. Найдите ошибку
4. Убедитесь, что команды выполняются из папки `frontend`

Если видите `cd frontend && npm install` в логах - значит Root Directory не настроен правильно!

