# 🔧 Исправление белого экрана на Vercel

## Проблема: Белый экран при открытии приложения

Это может быть из-за нескольких причин. Давайте исправим по порядку.

---

## Решение 1: Добавить vercel.json (уже сделано)

Я создал файл `vercel.json` в папке `frontend` для правильной работы роутинга.

Теперь нужно загрузить его на GitHub:

```bash
cd /Users/andreeviich/telegram-planner-app
git add frontend/vercel.json
git commit -m "Add vercel.json for routing"
git push
```

Vercel автоматически передеплоит с новой конфигурацией.

---

## Решение 2: Проверить консоль браузера

1. Откройте приложение в браузере: https://telegram-planner-app-egid.vercel.app
2. Нажмите F12 (или Cmd+Option+I на Mac)
3. Откройте вкладку **Console**
4. Посмотрите на ошибки (красным цветом)
5. Скопируйте ошибки и сообщите мне

---

## Решение 3: Проверить переменные окружения

1. Откройте проект в Vercel
2. Settings → Environment Variables
3. Убедитесь, что есть переменная:
   - `VITE_API_URL` = `https://your-backend-url.railway.app/api`
   - (замените на ваш реальный URL от Railway/Render)
4. Если переменной нет - добавьте её
5. Передеплойте проект после добавления

---

## Решение 4: Проверить сборку

1. В Vercel откройте вкладку **Deployments**
2. Найдите последний деплой
3. Откройте **Build Logs**
4. Проверьте, нет ли ошибок при сборке
5. Если есть ошибки - исправьте их

---

## Решение 5: Использовать HashRouter (альтернатива)

Если BrowserRouter не работает, можно использовать HashRouter:

1. Откройте `frontend/src/App.tsx`
2. Замените:
   ```typescript
   import { BrowserRouter as Router, Routes, Route } from 'react-router-dom'
   ```
   на:
   ```typescript
   import { HashRouter as Router, Routes, Route } from 'react-router-dom'
   ```

3. Закоммитьте и запушьте изменения
4. Vercel передеплоит автоматически

---

## Решение 6: Проверить базовый путь

Убедитесь, что в `vite.config.ts` правильно настроен базовый путь:

```typescript
export default defineConfig({
  base: '/',
  // ... остальное
})
```

---

## Быстрая диагностика

### Шаг 1: Проверьте в браузере
1. Откройте: https://telegram-planner-app-egid.vercel.app
2. F12 → Console
3. Какие ошибки видите?

### Шаг 2: Проверьте Network
1. F12 → Network
2. Обновите страницу
3. Проверьте, загружаются ли файлы (JS, CSS)
4. Если файлы не загружаются (404) - проблема в путях

### Шаг 3: Проверьте Source
1. F12 → Sources
2. Проверьте, есть ли файлы в `dist/` или `assets/`

---

## Частые причины белого экрана

1. **Ошибки JavaScript** - проверьте Console
2. **Неправильные пути к файлам** - проверьте Network
3. **Проблемы с роутингом** - нужен vercel.json (уже создан)
4. **Отсутствие переменных окружения** - проверьте Vercel Settings
5. **Ошибки при сборке** - проверьте Build Logs

---

## Что делать сейчас

1. ✅ Загрузите `vercel.json` на GitHub (команды выше)
2. ✅ Проверьте Console в браузере - какие ошибки?
3. ✅ Проверьте переменную `VITE_API_URL` в Vercel
4. ✅ Проверьте Build Logs в Vercel

После этого сообщите мне, что видите в Console - я помогу исправить конкретную ошибку!

