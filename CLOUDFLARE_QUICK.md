# ⚡ Быстрая настройка Cloudflare Pages

## Что я подготовил

✅ Обновил `vite.config.ts` - base path теперь `/` (для Cloudflare)  
✅ Создал файл `_redirects` для SPA routing  
✅ Подготовил инструкции  

---

## Пошаговая инструкция

### Шаг 1: Войдите в Cloudflare

1. Зайдите на [dash.cloudflare.com](https://dash.cloudflare.com)
2. Sign up / Login (можно через GitHub)

### Шаг 2: Создайте проект

1. **Pages** → **Create a project**
2. **Connect to Git** → выберите **GitHub**
3. Разрешите доступ → выберите репозиторий `telegram-planner-app`
4. **Begin setup**

### Шаг 3: Настройки сборки (ВАЖНО!)

```
Project name: flux-planner
Production branch: main

Framework preset: None  ← НЕ выбирайте Vite!
Build command: cd frontend && npm install && npm run build
Build output directory: frontend/dist
Root directory: frontend  ← или оставьте пустым
```

### Шаг 4: Переменные окружения

Добавьте:
- **NODE_VERSION** = `18`
- **VITE_API_URL** = ваш backend URL (например: `https://your-backend.railway.app/api`)

### Шаг 5: Деплой

1. **Save and Deploy**
2. Дождитесь завершения (2-3 минуты)
3. Получите URL: `https://flux-planner.pages.dev`

### Шаг 6: Обновите URL в BotFather

1. Скопируйте URL от Cloudflare
2. [@BotFather](https://t.me/botfather) → `/myapps`
3. Выберите приложение → **Edit Web App URL**
4. Вставьте новый URL

---

## Готово! 🎉

После этого приложение будет автоматически деплоиться при каждом push в `main`.

---

## Если что-то не работает

### Ошибка сборки
- Проверьте логи в Cloudflare
- Убедитесь, что настройки правильные (см. Шаг 3)

### 404 ошибка
- Файл `_redirects` уже создан
- Cloudflare автоматически поддерживает SPA routing

### Не загружается в Telegram
- Проверьте URL в BotFather
- Убедитесь, что URL начинается с `https://`

Сообщите, если нужна помощь!

