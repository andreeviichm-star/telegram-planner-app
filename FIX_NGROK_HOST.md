# 🔧 Исправление ошибки "Blocked request" для ngrok

## Проблема

Vite блокирует запросы с ngrok доменов по соображениям безопасности.

---

## Решение

Я обновил `vite.config.ts` чтобы разрешить ngrok домены. Теперь нужно:

### Шаг 1: Перезапустите Frontend

1. В терминале где запущен frontend нажмите **Ctrl+C**
2. Запустите снова:
   ```bash
   cd /Users/andreeviich/telegram-planner-app/frontend
   npm run dev
   ```

### Шаг 2: Проверьте работу

1. Откройте приложение через ngrok URL в Telegram
2. Должно работать без ошибок

---

## Что было исправлено

Добавлены настройки в `vite.config.ts`:
- `allowedHosts` - разрешает ngrok домены
- `hmr.clientPort` - правильный порт для HMR через ngrok

---

## Альтернатива: Разрешить все хосты (менее безопасно)

Если нужно разрешить любые хосты (для разработки):

```typescript
server: {
  port: 3000,
  host: true,
  allowedHosts: 'all'
}
```

Но лучше использовать конкретные домены как я сделал.

---

## Готово! 🎉

После перезапуска frontend приложение должно работать через ngrok без ошибок!

