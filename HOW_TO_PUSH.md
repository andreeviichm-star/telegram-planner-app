# 📤 Как загрузить изменения на GitHub

## Шаги

### 1. Проверьте статус

```bash
cd /Users/andreeviich/telegram-planner-app
git status
```

Покажет, какие файлы изменены.

### 2. Добавьте все изменения

```bash
git add .
```

Это добавит все измененные файлы.

### 3. Закоммитьте изменения

```bash
git commit -m "Add logging and fix API URL handling"
```

### 4. Запушите изменения

```bash
git push
```

---

## Все команды вместе

```bash
cd /Users/andreeviich/telegram-planner-app
git add .
git commit -m "Add logging and fix API URL handling"
git push
```

---

## После успешного push

1. **GitHub** → **Actions** → **Deploy to GitHub Pages**
2. Нажмите **"Run workflow"**
3. Выберите ветку: **main**
4. Нажмите **"Run workflow"**
5. Дождитесь завершения (2-3 минуты)
6. Проверьте сайт в Telegram

---

## Если будет ошибка

Если `git push` покажет ошибку, выполните:

```bash
git pull
```

Затем снова:

```bash
git push
```

Выполните команды выше в терминале!

