# 🔧 Исправление ошибки Vercel: "Command exited with 1"

## Проблема

Vercel пытается выполнить команды из корня проекта, но нужно работать из папки `frontend`.

---

## Решение 1: Настройка Root Directory в Vercel (рекомендуется)

### Шаг 1: Откройте настройки проекта в Vercel

1. Войдите в [vercel.com](https://vercel.com)
2. Откройте ваш проект `telegram-planner-app`
3. Откройте вкладку **Settings**

### Шаг 2: Настройте Root Directory

1. Найдите секцию **"General"**
2. Найдите поле **"Root Directory"**
3. Нажмите **"Edit"**
4. Введите: `frontend`
5. Нажмите **"Save"**

### Шаг 3: Передеплойте

1. Откройте вкладку **"Deployments"**
2. Найдите последний деплой
3. Нажмите на **три точки** (⋮) справа
4. Выберите **"Redeploy"**
5. Или создайте новый деплой: **"Deployments"** → **"Redeploy"**

---

## Решение 2: Исправленный vercel.json

Я уже обновил файл `vercel.json`. Теперь нужно:

1. Закоммитьте изменения:
```bash
cd /Users/andreeviich/telegram-planner-app
git add vercel.json
git commit -m "Fix Vercel configuration"
git push
```

2. Vercel автоматически передеплоит с новой конфигурацией

---

## Решение 3: Настройка вручную при создании проекта

Если создаете проект заново:

1. **Add New Project**
2. Выберите репозиторий
3. **Before deploying, configure your Project:**
   - **Root Directory**: нажмите **"Edit"** → введите `frontend`
   - **Framework Preset**: `Vite` (или оставьте "Other")
   - **Build Command**: `npm run build` (должно быть автоматически)
   - **Output Directory**: `dist` (должно быть автоматически)
   - **Install Command**: `npm install` (должно быть автоматически)

4. **Environment Variables**:
   - Добавьте `VITE_API_URL` = `https://your-backend-url.railway.app/api`

5. **Deploy**

---

## Решение 4: Использовать Vercel CLI

Если веб-интерфейс не работает:

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите
vercel login

# Перейдите в папку frontend
cd /Users/andreeviich/telegram-planner-app/frontend

# Деплой
vercel

# Следуйте инструкциям:
# - Set up and deploy? Y
# - Which scope? выберите ваш аккаунт
# - Link to existing project? N (или Y, если проект уже есть)
# - Project name? telegram-planner-app
# - Directory? ./ (текущая папка)
# - Override settings? N

# Добавьте переменную окружения
vercel env add VITE_API_URL production
# Введите значение: https://your-backend-url.railway.app/api

# Передеплойте
vercel --prod
```

---

## Проверка

После исправления:

1. Проверьте логи деплоя в Vercel
2. Должно быть: "Build Completed"
3. Откройте URL приложения
4. Должна открыться страница без ошибок

---

## Если все еще не работает

### Проверьте логи

1. В Vercel откройте вкладку **"Deployments"**
2. Нажмите на последний деплой
3. Откройте **"Build Logs"**
4. Найдите ошибку и сообщите мне

### Возможные проблемы:

1. **Ошибка npm install**: 
   - Проверьте, что `frontend/package.json` правильный
   - Убедитесь, что все зависимости указаны

2. **Ошибка TypeScript**:
   - Проверьте `frontend/tsconfig.json`
   - Возможно нужно исправить ошибки типов

3. **Ошибка build**:
   - Проверьте, что `npm run build` работает локально
   - Убедитесь, что все файлы на месте

---

## Быстрый чеклист

- [ ] Настроили Root Directory: `frontend` в Vercel
- [ ] Обновили vercel.json (уже сделано)
- [ ] Закоммитили и запушили изменения
- [ ] Передеплоили проект
- [ ] Добавили переменную `VITE_API_URL`
- [ ] Проверили логи деплоя

---

## Альтернатива: Деплой только frontend папки

Если ничего не помогает, можно создать отдельный репозиторий только для frontend:

1. Скопируйте папку `frontend` в новую папку
2. Создайте новый репозиторий на GitHub
3. Загрузите только frontend код
4. Деплойте этот репозиторий на Vercel (без Root Directory)

Но это крайний случай - обычно настройка Root Directory решает проблему.

