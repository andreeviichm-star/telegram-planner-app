# 🔍 Проверка деплоя на Railway

## Проблема: `/health` не возвращает статус

Если `https://telegram-planner-app-production.up.railway.app/health` не работает, проверьте:

## 1. Проверьте логи Railway

1. Откройте ваш проект в Railway
2. Перейдите на вкладку **"Deployments"** или **"Logs"**
3. Посмотрите последние логи - есть ли ошибки?

### Что искать в логах:
- ✅ `Server running on port ...` - сервер запустился
- ❌ Ошибки импорта модулей
- ❌ Ошибки подключения к базе данных
- ❌ Ошибки при инициализации

## 2. Проверьте структуру проекта на Railway

Railway должен знать, где находится бэкенд:

1. В Railway откройте настройки проекта
2. Проверьте **"Root Directory"** или **"Source"**
3. Должно быть указано: `backend` (если проект в папке backend)

## 3. Проверьте команду запуска

Railway должен знать, как запустить сервер:

1. В Railway откройте настройки сервиса
2. Проверьте **"Start Command"** или **"Build Command"**
3. Должно быть что-то вроде:
   ```
   npm start
   ```
   или
   ```
   node server.js
   ```

## 4. Проверьте package.json

Убедитесь, что в `backend/package.json` есть:

```json
{
  "type": "module",
  "scripts": {
    "start": "node server.js"
  }
}
```

## 5. Проверьте переменные окружения

В Railway должны быть установлены:
- `PORT` - Railway устанавливает автоматически
- Другие переменные, если нужны (например, для базы данных)

## 6. Проверьте, что сервис запущен

1. В Railway откройте ваш сервис
2. Проверьте статус - должен быть **"Active"** или **"Running"**
3. Если статус **"Stopped"** или **"Error"** - перезапустите

## 7. Попробуйте перезапустить сервис

1. В Railway найдите кнопку **"Redeploy"** или **"Restart"**
2. Нажмите её
3. Дождитесь завершения деплоя
4. Проверьте логи

## 8. Проверьте URL

Убедитесь, что используете правильный URL:
- ✅ `https://telegram-planner-app-production.up.railway.app/health`
- ❌ Не используйте `/api/health` (это только для API endpoints)

## Что делать дальше:

1. **Проверьте логи Railway** - там должна быть причина ошибки
2. **Убедитесь, что Root Directory = `backend`** (если проект в папке backend)
3. **Проверьте Start Command** - должен быть `npm start` или `node server.js`
4. **Перезапустите сервис** в Railway

## Если всё равно не работает:

Пришлите:
1. Логи из Railway (последние 50-100 строк)
2. Настройки проекта (Root Directory, Start Command)
3. Содержимое `backend/package.json`

