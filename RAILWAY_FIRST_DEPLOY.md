# 🚀 Первый деплой на Railway

## Проблема: "There is no active deployment for this service"

Это означает, что сервис еще не был задеплоен. Нужно создать первый деплой.

## Решение: Подключите репозиторий или загрузите код

### Вариант 1: Подключить GitHub репозиторий (рекомендуется)

1. **В Railway откройте ваш проект**
2. **Нажмите "New" → "GitHub Repo"** (или найдите кнопку подключения GitHub)
3. **Выберите репозиторий**: `andreeviichm-star/telegram-planner-app`
4. **Railway автоматически:**
   - Подключит репозиторий
   - Определит структуру проекта
   - Начнет деплой

5. **После подключения:**
   - Railway автоматически начнет деплой
   - Дождитесь завершения (обычно 2-5 минут)
   - Проверьте логи

### Вариант 2: Настроить деплой вручную

Если репозиторий уже подключен, но деплой не запускается:

1. **Проверьте настройки сервиса:**
   - Root Directory: `backend`
   - Start Command: `npm start`
   - Build Command: (можно оставить пустым)

2. **Запустите деплой вручную:**
   - Найдите кнопку **"Deploy"** или **"Redeploy"**
   - Нажмите её
   - Дождитесь завершения

3. **Или сделайте commit в GitHub:**
   ```bash
   cd /Users/andreeviich/telegram-planner-app
   git add .
   git commit -m "Trigger Railway deploy"
   git push
   ```
   Railway автоматически задеплоит изменения

### Вариант 3: Загрузить код напрямую (если нет GitHub)

1. **В Railway нажмите "New" → "Empty Project"**
2. **Нажмите "Deploy from GitHub repo"**
3. **Или используйте Railway CLI:**
   ```bash
   npm install -g @railway/cli
   railway login
   railway init
   railway up
   ```

## После подключения репозитория

### Проверьте настройки:

1. **Root Directory:**
   - Должно быть: `backend`
   - Это указывает Railway, где находится код бэкенда

2. **Start Command:**
   - Должно быть: `npm start`
   - Или: `node server.js`

3. **Build Command:**
   - Можно оставить пустым
   - Или: `npm install`

### Railway автоматически:

1. Установит зависимости (`npm install`)
2. Запустит сервер (`npm start`)
3. Создаст деплой

## Проверка деплоя

1. **Откройте вкладку "Deployments"**
2. Должен появиться новый деплой со статусом:
   - 🟡 "Building" - идет сборка
   - 🟢 "Active" - деплой успешен
   - 🔴 "Failed" - ошибка (проверьте логи)

3. **Откройте вкладку "Logs"**
4. Должны увидеть:
   ```
   Server running on port 3001
   ```
   или
   ```
   Server running on port <PORT>
   ```

## Если деплой не запускается автоматически

1. **Проверьте, что репозиторий подключен:**
   - Settings → Source
   - Должен быть указан ваш GitHub репозиторий

2. **Проверьте настройки деплоя:**
   - Settings → Deploy
   - Branch: `main` (или ваша основная ветка)
   - Root Directory: `backend`

3. **Запустите деплой вручную:**
   - Нажмите "Deploy" или "Redeploy"
   - Или сделайте commit в GitHub

## После успешного деплоя

1. **Проверьте health endpoint:**
   ```
   https://telegram-planner-app-production.up.railway.app/health
   ```
   Должен вернуться: `{"status":"ok"}`

2. **Обновите секрет VITE_API_URL в GitHub:**
   ```
   https://telegram-planner-app-production.up.railway.app/api
   ```

3. **Перезапустите GitHub Pages workflow**

