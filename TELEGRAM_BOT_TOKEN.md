# 🤖 Telegram Bot Token

## Нужен ли токен для Mini App?

**Короткий ответ: НЕТ, для Mini App токен не нужен.**

Mini App работает через WebView и не требует токена бота.

## Когда токен нужен?

Токен нужен только если вы хотите:
- ✅ Отправлять уведомления через Telegram бота
- ✅ Использовать Bot API для взаимодействия с пользователями
- ✅ Обрабатывать команды бота (например, `/start`, `/help`)

## Текущая ситуация

В вашем проекте:
- ✅ Mini App работает без токена
- ❌ Уведомления через бота не настроены (пока)

## Если хотите добавить уведомления через бота:

### 1. Добавьте токен в переменные окружения

В Render:
1. Откройте ваш сервис
2. Перейдите в "Environment"
3. Добавьте переменную:
   - **Key**: `TELEGRAM_BOT_TOKEN`
   - **Value**: `8203311307:AAGjMsjPyy3Dfaj11gSoRt_hd4J11cxTJoE`

### 2. Создайте файл для бота (опционально)

Если хотите использовать бота для уведомлений, можно создать `backend/bot.js`:

```javascript
import TelegramBot from 'node-telegram-bot-api'

const token = process.env.TELEGRAM_BOT_TOKEN
if (!token) {
  console.log('Bot token not provided, bot disabled')
} else {
  const bot = new TelegramBot(token, { polling: true })
  
  bot.on('message', (msg) => {
    console.log('Bot message:', msg.text)
  })
  
  // Функция для отправки уведомлений
  export const sendNotification = async (chatId, message) => {
    if (token && bot) {
      await bot.sendMessage(chatId, message)
    }
  }
}
```

### 3. Используйте в server.js

```javascript
import { sendNotification } from './bot.js'

// В роуте уведомлений
app.post('/api/notifications', async (req, res) => {
  // ... создание уведомления
  await sendNotification(userChatId, 'У вас новая задача!')
  res.json({ success: true })
})
```

## Рекомендация:

**Для текущего функционала токен НЕ нужен.**

Mini App работает без токена. Токен понадобится только если вы хотите:
- Отправлять уведомления через бота
- Обрабатывать команды бота

## Безопасность:

⚠️ **НЕ коммитьте токен в Git!**

Используйте только переменные окружения:
- ✅ В Render: Environment Variables
- ✅ В локальной разработке: `.env` файл (добавьте в `.gitignore`)

## Текущий статус:

- ✅ Mini App работает без токена
- ✅ API работает на Render
- ❌ Уведомления через бота не настроены (но это не критично)

**Вывод: Токен не нужен для текущего функционала. Оставьте его для будущего, если захотите добавить уведомления через бота.**

