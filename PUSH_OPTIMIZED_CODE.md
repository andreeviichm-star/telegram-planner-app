# 📤 Загрузка оптимизированного кода на GitHub

## Шаги

### 1. Перейдите в папку проекта

```bash
cd /Users/andreeviich/telegram-planner-app
```

### 2. Проверьте статус

```bash
git status
```

Покажет все измененные файлы.

### 3. Добавьте все изменения

```bash
git add .
```

### 4. Закоммитьте изменения

```bash
git commit -m "Optimize code: add logger, telegram utils, improve performance"
```

### 5. Запушите изменения

```bash
git push
```

---

## Все команды вместе

```bash
cd /Users/andreeviich/telegram-planner-app
git add .
git commit -m "Optimize code: add logger, telegram utils, improve performance"
git push
```

---

## Если будет ошибка

Если `git push` покажет ошибку (например, "non-fast-forward"), выполните:

```bash
git pull
```

Затем снова:

```bash
git push
```

---

## После успешного push

1. **GitHub** → **Actions** → **Deploy to GitHub Pages**
2. Нажмите **"Run workflow"**
3. Выберите ветку: **main**
4. Нажмите **"Run workflow"**
5. Дождитесь завершения (2-3 минуты)
6. Проверьте сайт в Telegram

---

## Что будет после деплоя

- ✅ Логи только в dev режиме (чистая консоль в production)
- ✅ Оптимизированная производительность
- ✅ Правильная обработка ошибок
- ✅ Автоматическое исправление API URL

Выполните команды выше в терминале!

