# 🔌 Какой порт выбрать в Railway

## Ответ: Обычно порт выбирать НЕ нужно!

Railway автоматически определяет порт через переменную окружения `PORT`.

## Если Railway все же спрашивает порт:

### Вариант 1: Оставьте по умолчанию
- Railway обычно предлагает порт автоматически
- Просто нажмите "Generate Domain" без выбора порта

### Вариант 2: Если нужно выбрать вручную

1. **Проверьте переменную окружения PORT в Railway:**
   - Откройте ваш сервис в Railway
   - Перейдите в "Variables"
   - Найдите переменную `PORT`
   - Если её нет, Railway использует порт по умолчанию (обычно 3000)

2. **В коде бэкенда:**
   ```javascript
   const PORT = process.env.PORT || 3001
   ```
   - Если `PORT` не установлен, используется 3001
   - Но Railway обычно устанавливает `PORT` автоматически

3. **При генерации домена:**
   - Если Railway показывает список портов, выберите тот, который соответствует вашему сервису
   - Обычно это порт, указанный в переменной `PORT`
   - Или просто выберите порт по умолчанию (Railway обычно предлагает правильный)

## Рекомендация:

**Просто нажмите "Generate Domain"** - Railway автоматически настроит всё правильно!

После генерации домена Railway создаст URL вида:
```
https://your-app-name-production.up.railway.app
```

Этот URL уже будет работать на правильном порту (Railway обрабатывает это автоматически).

## Важно:

- ✅ Не нужно указывать порт в URL (Railway обрабатывает это автоматически)
- ✅ URL будет вида: `https://your-app-name.railway.app` (без порта)
- ✅ Railway автоматически направляет трафик на правильный порт

