# Telegram Mini App - Планировщик задач и дневник

Полнофункциональное Telegram Mini App приложение для планирования задач и ведения дневника.

## Функционал

### Планировщик задач
- ✅ Создание и управление задачами
- ✅ Встречи и события
- ✅ Система приоритетов (низкий, средний, высокий, срочный)
- ✅ Оценка времени выполнения задач
- ✅ Делегирование задач другим пользователям
- ✅ Прикрепление документов и отчетов
- ✅ Фильтрация по приоритету и статусу

### Дневник (Календарь)
- 📅 Отметки месячных
- 🟡 Отметки овуляции
- 💜 Отметки секса
- 📝 Заметки к событиям

### Дизайн
- 🌙 Темная тема
- 🎨 Градиенты и акцентный синий цвет
- ✨ Liquid glass эффект (glassmorphism)
- 📱 Адаптивный интерфейс

### Уведомления
- 🔔 Система уведомлений для задач и событий

## Технологии

### Frontend
- React 18
- TypeScript
- Vite
- React Router
- Telegram WebApp SDK
- date-fns
- Lucide React (иконки)

### Backend
- Node.js
- Express
- SQLite
- Telegram Bot API

## Установка

1. Установите зависимости:
```bash
npm run install:all
```

2. Настройте переменные окружения:
```bash
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
# Отредактируйте backend/.env и добавьте токен бота (опционально)
```

## 🚀 Быстрый запуск в Telegram

### Вариант 1: Автоматический скрипт (рекомендуется)

```bash
./start-dev.sh
```

Скрипт автоматически:
- Проверит зависимости
- Запустит backend и frontend
- Запустит ngrok туннель
- Покажет инструкции

### Вариант 2: Ручной запуск

1. **Создайте Telegram бота:**
   - Откройте [@BotFather](https://t.me/botfather) в Telegram
   - Отправьте `/newbot` и следуйте инструкциям
   - Сохраните токен бота

2. **Создайте Mini App:**
   - В BotFather отправьте `/newapp`
   - Выберите вашего бота
   - Заполните информацию (Web App URL пока оставьте пустым)

3. **Установите ngrok** (для локального туннеля):
   ```bash
   brew install ngrok
   # или скачайте с https://ngrok.com/download
   ```

4. **Запустите backend:**
   ```bash
   cd backend
   npm install
   npm run dev
   ```

5. **Запустите frontend** (в другом терминале):
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

6. **Запустите ngrok** (в третьем терминале):
   ```bash
   ngrok http 3000
   ```

7. **Обновите Web App URL в BotFather:**
   - Откройте http://localhost:4040 (веб-интерфейс ngrok)
   - Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
   - В BotFather: `/myapps` → выберите приложение → Edit → Web App URL
   - Вставьте URL от ngrok

8. **Откройте бота в Telegram:**
   - Найдите вашего бота
   - Нажмите кнопку меню или отправьте `/start`
   - Нажмите "Open App" или "Launch App"

📖 **Подробная инструкция:** см. [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md)

## Структура проекта

```
├── frontend/          # React приложение
│   ├── src/
│   │   ├── components/  # Компоненты UI
│   │   ├── pages/       # Страницы
│   │   ├── services/    # API сервисы
│   │   └── types/       # TypeScript типы
│   └── package.json
├── backend/           # Node.js API
│   ├── routes/         # API роуты
│   ├── db.js           # База данных
│   └── server.js       # Сервер
└── package.json
```

## API Endpoints

### Tasks
- `GET /api/tasks` - Получить все задачи
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить задачу
- `DELETE /api/tasks/:id` - Удалить задачу

### Calendar
- `GET /api/calendar/events` - Получить события календаря
- `POST /api/calendar/events` - Создать событие
- `POST /api/calendar/events/:id` - Обновить событие
- `DELETE /api/calendar/events/:id` - Удалить событие

### Notifications
- `GET /api/notifications` - Получить уведомления
- `POST /api/notifications` - Создать уведомление

## Деплой

### Frontend (Vercel/Netlify)
1. Подключите репозиторий
2. Укажите build команду: `cd frontend && npm run build`
3. Укажите output directory: `frontend/dist`
4. Добавьте переменную окружения: `VITE_API_URL=https://your-api-url.com/api`

### Backend (Railway/Heroku)
1. Подключите репозиторий
2. Укажите start команду: `cd backend && npm start`
3. Добавьте переменные окружения из `.env`

## Лицензия

MIT

