# 🔍 Проверка backend для исправления белого экрана

## По логам Telegram видно

✅ Страница загружается  
✅ Ресурсы запрашиваются  
✅ Telegram WebApp инициализируется  
⚠️ Но приложение может показывать белый экран  

## Вероятная причина

**Backend недоступен** - приложение пытается загрузить данные, но не может, и это может вызывать белый экран.

---

## Что проверить

### 1. Работает ли backend?

1. Откройте backend URL в браузере:
   - `https://your-backend.railway.app/health`
2. Должно показать: `{"status":"ok"}`
3. Если не работает - проблема с backend

### 2. Правильно ли настроен VITE_API_URL?

1. GitHub → **Settings** → **Secrets and variables** → **Actions**
2. Проверьте секрет `VITE_API_URL`:
   - ✅ Должен быть добавлен
   - ✅ Значение должно быть правильным
   - ✅ Должен заканчиваться на `/api` (например: `https://your-backend.railway.app/api`)

### 3. Перезапустите workflow

После проверки/исправления секрета:

1. **Actions** → **Deploy to GitHub Pages**
2. Нажмите **"Run workflow"**
3. Дождитесь завершения
4. Проверьте сайт снова

---

## Что я исправил

✅ Улучшил обработку ошибок во всех страницах:
- `TasksPage` - теперь показывает пустой список, если backend недоступен
- `MeetingsPage` - теперь показывает пустой список, если backend недоступен
- `CalendarPage` - теперь показывает пустой список, если backend недоступен
- `BudgetPage` - теперь показывает пустой список, если backend недоступен

Теперь приложение должно работать даже если backend недоступен (покажет пустые списки).

---

## После исправлений

1. ✅ **Backend доступен** (`/health` работает)
2. ✅ **Секрет `VITE_API_URL` правильный** (заканчивается на `/api`)
3. ✅ **Workflow перезапущен** (после исправления секрета)
4. ✅ **Приложение работает** (даже если backend недоступен, покажет пустые списки)

---

## Сообщите

1. **Работает ли backend?** (`/health` показывает `{"status":"ok"}`?)
2. **Добавлен ли секрет `VITE_API_URL`?** (и какое значение?)
3. **Что показывает сайт в браузере на телефоне?** (работает/белый экран/ошибка)

Это поможет точно определить проблему!

