# 🔧 Исправление: deploy.yml не отображается в Actions

## Проблема
Workflow `deploy.yml` не отображается в Actions на GitHub.

## Возможные причины
1. Файл не загружен на GitHub
2. Файл имеет неправильный синтаксис
3. Workflow не запускался (не было подходящих коммитов)

---

## Решение

### Шаг 1: Проверьте, загружен ли файл на GitHub

1. Откройте репозиторий: `https://github.com/andreeviichm-star/telegram-planner-app`
2. Перейдите в `.github/workflows/`
3. Проверьте, есть ли файл `deploy.yml`
4. Если файла нет - загрузите его (см. Шаг 2)
5. Если файл есть - проверьте его содержимое

### Шаг 2: Загрузите файл на GitHub

#### Вариант A: Через веб-интерфейс

1. Откройте `.github/workflows/` на GitHub
2. Нажмите **Add file** → **Create new file**
3. Имя файла: `deploy.yml`
4. Скопируйте содержимое из файла ниже
5. Нажмите **Commit new file**

#### Вариант B: Через git

```bash
cd /Users/andreeviich/telegram-planner-app

# Синхронизируйте с GitHub
git pull --rebase

# Добавьте workflow файл
git add .github/workflows/deploy.yml

# Создайте коммит
git commit -m "Add GitHub Pages deployment workflow"

# Загрузите на GitHub
git push
```

### Шаг 3: Обновите workflow (добавлен ручной запуск)

Я обновил workflow, чтобы его можно было запускать вручную. Теперь он запускается:
- При любом push в ветку `main`
- Вручную через Actions (кнопка "Run workflow")

### Шаг 4: Запустите workflow вручную

1. Репозиторий → **Actions**
2. В левом меню найдите **"Deploy to GitHub Pages"**
3. Если его нет - обновите страницу (F5)
4. Нажмите на workflow
5. Нажмите **"Run workflow"** (справа вверху)
6. Выберите ветку: **main**
7. Нажмите **"Run workflow"**

### Шаг 5: Дождитесь завершения

1. В **Actions** дождитесь завершения workflow
2. Должен быть зеленый статус ✅
3. После этого сайт должен обновиться

---

## Проверка

После выполнения шагов:

1. ✅ Файл `deploy.yml` загружен на GitHub
2. ✅ Workflow отображается в Actions
3. ✅ Workflow успешно выполнился
4. ✅ GitHub Pages показывает приложение

---

## Если все еще не работает

### Проверьте синтаксис YAML

1. Откройте файл `.github/workflows/deploy.yml` на GitHub
2. Проверьте, нет ли ошибок (красные подчеркивания)
3. Убедитесь, что отступы правильные (пробелы, не табы)

### Проверьте секреты

1. **Settings** → **Secrets and variables** → **Actions**
2. Убедитесь, что секрет `VITE_API_URL` добавлен

### Проверьте настройки Pages

1. **Settings** → **Pages**
2. Убедитесь, что **Source** = **"GitHub Actions"**

---

## Готово!

После выполнения всех шагов workflow должен появиться в Actions и успешно задеплоить приложение.

Сообщите, что видите после выполнения шагов!

