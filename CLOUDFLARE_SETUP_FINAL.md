# ⚡ Настройка Cloudflare Pages - Пошаговая инструкция

## Подготовка

Я подготовил все необходимые файлы. Теперь нужно настроить проект в Cloudflare.

---

## Шаг 1: Регистрация/Вход в Cloudflare

1. Зайдите на [dash.cloudflare.com](https://dash.cloudflare.com)
2. Sign up (если нет аккаунта) или Login
3. Можно войти через GitHub для удобства

---

## Шаг 2: Создание проекта

1. В левом меню выберите **Pages**
2. Нажмите **Create a project**
3. Выберите **Connect to Git**
4. Выберите **GitHub** (или GitLab, если используете)
5. Разрешите доступ к репозиторию
6. Выберите ваш репозиторий: `telegram-planner-app`
7. Нажмите **Begin setup**

---

## Шаг 3: Настройка сборки (ВАЖНО!)

Заполните следующие поля:

### Основные настройки:
- **Project name:** `flux-planner` (или любое другое имя)
- **Production branch:** `main`

### Настройки сборки:
- **Framework preset:** `None` (НЕ выбирайте Vite!)
- **Build command:** `cd frontend && npm install && npm run build`
- **Build output directory:** `frontend/dist`
- **Root directory:** `frontend` (оставьте пустым или укажите `frontend`)

### Переменные окружения:
Нажмите **Add environment variable** и добавьте:

1. **NODE_VERSION**
   - Value: `18`

2. **VITE_API_URL**
   - Value: ваш backend URL (например: `https://your-backend.railway.app/api`)

---

## Шаг 4: Деплой

1. Нажмите **Save and Deploy**
2. Дождитесь завершения сборки (2-3 минуты)
3. После успешного деплоя вы получите URL:
   - `https://flux-planner.pages.dev` (или другое имя, которое вы указали)

---

## Шаг 5: Обновить URL в BotFather

1. Скопируйте URL от Cloudflare Pages
2. Откройте [@BotFather](https://t.me/botfather)
3. Отправьте `/myapps`
4. Выберите ваше приложение
5. Выберите **Edit Web App URL**
6. Вставьте новый URL от Cloudflare
7. Готово!

---

## Шаг 6: Проверка работы

1. Откройте URL в браузере - должна открыться страница приложения
2. Откройте приложение в Telegram - должно работать
3. Проверьте консоль (F12) - не должно быть ошибок

---

## Если сборка не работает

### Проблема: "Build failed"
**Решение:**
1. Проверьте логи сборки в Cloudflare
2. Убедитесь, что:
   - `Root directory` = `frontend` (или пусто)
   - `Build command` = `cd frontend && npm install && npm run build`
   - `Build output directory` = `frontend/dist`

### Проблема: "Cannot find module"
**Решение:**
1. Убедитесь, что `package.json` находится в папке `frontend`
2. Проверьте, что все зависимости указаны в `package.json`

### Проблема: "404 Not Found"
**Решение:**
1. Cloudflare Pages автоматически поддерживает SPA routing
2. Файл `_redirects` уже создан и должен работать

---

## Преимущества Cloudflare Pages

✅ Работает в России  
✅ Быстрый CDN по всему миру  
✅ Бесплатный тариф  
✅ Автоматический деплой из GitHub  
✅ HTTPS по умолчанию  
✅ Поддержка SPA routing  
✅ Нет проблем с токенами (как в GitHub Actions)  

---

## После настройки

При каждом push в ветку `main` Cloudflare автоматически:
1. Обнаружит изменения
2. Запустит сборку
3. Задеплоит новую версию

Обычно это занимает 2-3 минуты.

---

## Готово! 🎉

После выполнения всех шагов приложение будет работать на Cloudflare Pages.

Если возникнут проблемы - сообщите, помогу исправить!

