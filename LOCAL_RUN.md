# 🚀 Локальный запуск приложения

## Быстрый запуск

### Шаг 1: Установите зависимости (если еще не установлены)

```bash
cd /Users/andreeviich/telegram-planner-app
npm run install:all
```

Или вручную:

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

---

## Шаг 2: Запустите Backend

Откройте **Терминал 1**:

```bash
cd /Users/andreeviich/telegram-planner-app/backend
npm run dev
```

Должно появиться:
```
Server running on port 3001
```

Backend будет доступен на: `http://localhost:3001`

---

## Шаг 3: Запустите Frontend

Откройте **Терминал 2**:

```bash
cd /Users/andreeviich/telegram-planner-app/frontend
npm run dev
```

Должно появиться:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

Frontend будет доступен на: `http://localhost:3000`

---

## Шаг 4: Установите и запустите ngrok

### Установка ngrok

```bash
# macOS
brew install ngrok

# Или скачайте с https://ngrok.com/download
```

### Запуск ngrok

Откройте **Терминал 3**:

```bash
ngrok http 3000
```

Вы увидите что-то вроде:
```
Forwarding  https://xxxx-xx-xx-xx-xx.ngrok-free.app -> http://localhost:3000
```

**Скопируйте HTTPS URL** (например: `https://abc123.ngrok-free.app`)

---

## Шаг 5: Обновите Web App URL в BotFather

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/botfather)
3. Отправьте: `/myapps`
4. Выберите ваше приложение
5. Выберите **"Edit"**
6. Выберите **"Web App URL"**
7. Вставьте HTTPS URL от ngrok (например: `https://abc123.ngrok-free.app`)
8. Подтвердите

---

## Шаг 6: Откройте приложение в Telegram

1. Найдите вашего бота в Telegram
2. Нажмите меню или отправьте `/start`
3. Нажмите **"Open App"** или **"Launch App"**
4. Должно открыться ваше Mini App! 🎉

---

## Проверка работы

### Backend
- Откройте: http://localhost:3001/health
- Должно показать: `{"status":"ok"}`

### Frontend
- Откройте: http://localhost:3000
- Должна открыться страница приложения

### API
- Frontend должен подключаться к: `http://localhost:3001/api`
- Проверьте `frontend/.env` - должен быть: `VITE_API_URL=http://localhost:3001/api`

---

## Автоматический запуск (скрипт)

Я уже создал скрипт `start-dev.sh`. Запустите:

```bash
cd /Users/andreeviich/telegram-planner-app
./start-dev.sh
```

Скрипт автоматически:
- Проверит зависимости
- Запустит backend
- Запустит frontend
- Запустит ngrok
- Покажет инструкции

---

## Остановка

Чтобы остановить все процессы:

1. В каждом терминале нажмите **Ctrl+C**
2. Или закройте терминалы

---

## Решение проблем

### Backend не запускается
- Проверьте, что порт 3001 свободен: `lsof -i :3001`
- Убедитесь, что установлены зависимости: `cd backend && npm install`

### Frontend не запускается
- Проверьте, что порт 3000 свободен: `lsof -i :3000`
- Убедитесь, что установлены зависимости: `cd frontend && npm install`

### ngrok не работает
- Убедитесь, что ngrok установлен: `ngrok version`
- Проверьте, что frontend запущен на порту 3000

### Приложение не открывается в Telegram
- Проверьте, что ngrok запущен
- Убедитесь, что URL правильно вставлен в BotFather
- Проверьте консоль браузера (в Telegram: долгое нажатие → "Inspect Element")

---

## Полезные команды

```bash
# Проверить, что работает
curl http://localhost:3001/health
curl http://localhost:3000

# Остановить процессы на портах
lsof -ti:3000 | xargs kill -9
lsof -ti:3001 | xargs kill -9

# Проверить логи
# Backend логи видны в терминале где запущен npm run dev
# Frontend логи видны в терминале где запущен npm run dev
```

---

## Готово! 🎉

Теперь приложение работает локально и доступно в Telegram через ngrok!

**Важно:** ngrok работает только пока запущен. Если закрыть терминал с ngrok - приложение перестанет быть доступным в Telegram.

