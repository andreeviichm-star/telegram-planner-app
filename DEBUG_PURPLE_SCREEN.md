# 🔍 Отладка: только фиолетовый фон

## Что проверить

### 1. В консоли должны быть логи

Проверьте, есть ли эти логи:
- `🚀 App component mounted`
- `📐 Layout component mounted`
- `📋 TasksPage component mounted`
- `🔗 Final API URL: ...`

**Если этих логов НЕТ** - компоненты не загружаются.

### 2. В Network tab проверьте

1. Откройте DevTools → **Network**
2. Обновите страницу
3. Проверьте:
   - ✅ Загружаются ли CSS файлы? (`assets/index-*.css`)
   - ✅ Загружаются ли JS файлы? (`assets/index.js`)
   - ❌ Есть ли ошибки 404?

**Если CSS файлы не загружаются** - проблема с путями.

### 3. В Elements tab проверьте

1. Откройте DevTools → **Elements**
2. Найдите `<div id="root">`
3. Что внутри?
   - ✅ Есть элементы? (Layout, TasksPage и т.д.)
   - ❌ Пусто? (только комментарии React)

**Если пусто** - React не рендерит компоненты.

---

## Возможные проблемы

### Проблема 1: CSS не загружается

Если CSS файлы не загружаются (404 ошибки):
- Проблема с base path
- Нужно проверить vite.config.ts

### Проблема 2: Компоненты не рендерятся

Если компоненты не рендерятся:
- Проблема с HashRouter и base path
- Возможно нужно использовать BrowserRouter

### Проблема 3: Компоненты рендерятся, но не видны

Если компоненты есть в DOM, но не видны:
- Проблема с CSS (z-index, display, visibility)
- Проблема с размерами (width: 0, height: 0)

---

## Сообщите

1. **Есть ли логи компонентов?** (📐 Layout, 📋 TasksPage)
2. **Загружаются ли CSS файлы?** (проверьте Network tab)
3. **Что внутри `<div id="root">`?** (проверьте Elements tab)
4. **Есть ли ошибки в консоли?** (кроме WebSocket)

Это поможет точно определить проблему!

