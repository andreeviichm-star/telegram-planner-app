# 🔍 Отладка проблем с загрузкой в Telegram

## Проблема: Сайт не загружается в Telegram боте с телефона

Это может быть связано с несколькими причинами. Давайте проверим по порядку.

---

## Шаг 1: Проверьте URL в BotFather

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/myapps`
3. Выберите ваше приложение
4. Проверьте **Web App URL**
5. Убедитесь, что URL правильный и начинается с `https://`

**Важно:** URL должен быть HTTPS, не HTTP!

---

## Шаг 2: Проверьте работу сайта в браузере

1. Откройте URL от Netlify/Vercel в браузере на компьютере
2. Должна открыться страница приложения
3. Если не открывается - проблема на стороне хостинга

---

## Шаг 3: Проверьте консоль в Telegram

1. Откройте бота в Telegram на телефоне
2. Нажмите "Open App"
3. Если приложение не открывается, попробуйте:
   - Долгое нажатие на приложение → "Inspect Element" (если доступно)
   - Или откройте в браузере на телефоне

4. Проверьте консоль на ошибки:
   - Network errors (404, 500, CORS)
   - JavaScript errors
   - MIME type errors

---

## Шаг 4: Проверьте CORS на backend

Убедитесь, что backend разрешает запросы с вашего домена.

Проверьте `backend/server.js` - должно быть:
```javascript
app.use(cors())
```

Или более строго:
```javascript
app.use(cors({
  origin: [
    'https://your-app.netlify.app',
    'https://your-app.vercel.app',
    'https://telegram.org'
  ]
}))
```

---

## Шаг 5: Проверьте переменные окружения

### Frontend (Netlify/Vercel)
- `VITE_API_URL` должен указывать на ваш backend URL
- Например: `https://your-backend.railway.app/api`

### Backend (Railway/Render)
- `PORT` должен быть установлен
- `TELEGRAM_BOT_TOKEN` (опционально)

---

## Шаг 6: Проверьте доступность backend

1. Откройте в браузере: `https://your-backend-url.railway.app/health`
2. Должно показать: `{"status":"ok"}`
3. Если не работает - проблема с backend

---

## Шаг 7: Проверьте логи

### Netlify/Vercel
1. Откройте проект в панели управления
2. Вкладка **Deployments**
3. Посмотрите на последний деплой
4. Проверьте **Build Logs** на ошибки

### Backend (Railway/Render)
1. Откройте проект в панели управления
2. Вкладка **Logs** или **Deployments**
3. Проверьте логи на ошибки

---

## Частые проблемы и решения

### Проблема 1: "Failed to load resource"
- Проверьте, что backend доступен
- Проверьте `VITE_API_URL` в frontend
- Проверьте CORS на backend

### Проблема 2: "CORS error"
- Убедитесь, что `cors()` включен в backend
- Проверьте, что backend разрешает ваш домен

### Проблема 3: "MIME type error"
- Мы уже исправили это, но проверьте логи сборки
- Убедитесь, что файлы загружены на GitHub

### Проблема 4: Белый экран
- Откройте консоль браузера (F12)
- Проверьте ошибки JavaScript
- Проверьте Network tab - загружаются ли файлы

### Проблема 5: "Network error"
- Проверьте интернет-соединение
- Проверьте, что backend работает
- Проверьте URL в BotFather

---

## Быстрая диагностика

Выполните эти проверки:

1. ✅ URL в BotFather правильный и HTTPS?
2. ✅ Сайт открывается в браузере на компьютере?
3. ✅ Backend доступен (`/health` работает)?
4. ✅ CORS настроен правильно?
5. ✅ Переменные окружения установлены?
6. ✅ Нет ошибок в логах деплоя?

---

## Если ничего не помогает

Попробуйте:

1. **Пересоздать проект в Netlify/Vercel**
   - Удалите старый проект
   - Создайте новый с теми же настройками

2. **Использовать другой хостинг**
   - Cloudflare Pages
   - GitHub Pages

3. **Проверить на другом устройстве**
   - Попробуйте открыть на другом телефоне
   - Или в браузере на компьютере

---

## Что проверить сейчас

1. Откройте URL от Netlify в браузере на компьютере - работает?
2. Откройте URL от Netlify в браузере на телефоне - работает?
3. Какие ошибки видите в консоли (если доступна)?

Сообщите результаты - помогу исправить конкретную проблему!

