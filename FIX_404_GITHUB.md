# 🔧 Исправление ошибки 404 на GitHub Pages

## Проблема
```
Failed to load resource: the server responded with a status of 404
```

## Причины

1. **Неправильный base path** - путь к ресурсам не совпадает с URL репозитория
2. **Неправильная настройка GitHub Pages** - неверный source или путь
3. **Кэш браузера** - старые пути в кэше

---

## Решение

### Вариант 1: Использовать правильный base path

Проверьте URL вашего репозитория:
- Если репозиторий: `telegram-planner-app`
- То URL будет: `https://username.github.io/telegram-planner-app/`
- Base path должен быть: `/telegram-planner-app/`

### Вариант 2: Использовать корневой домен (если есть)

Если у вас есть собственный домен или вы хотите использовать `username.github.io` без пути:
- Base path должен быть: `/`

---

## Что нужно сделать

### Шаг 1: Проверьте URL GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Settings → Pages
3. Посмотрите URL (например: `https://andreeviichm-star.github.io/telegram-planner-app/`)

### Шаг 2: Обновите base path в vite.config.ts

Если URL: `https://username.github.io/telegram-planner-app/`
То base path: `/telegram-planner-app/`

Если URL: `https://username.github.io/`
То base path: `/`

### Шаг 3: Пересоберите и задеплойте

После изменения base path нужно пересобрать проект.

---

## Быстрое исправление

Я обновлю `vite.config.ts` чтобы автоматически определять правильный base path.

Если это не поможет, попробуйте:

1. **Очистить кэш браузера** (Ctrl+Shift+R или Cmd+Shift+R)
2. **Проверить настройки GitHub Pages**
3. **Убедиться, что все файлы загружены**

---

## Альтернатива: Использовать другой хостинг

Если GitHub Pages продолжает вызывать проблемы, можно использовать:
- **Cloudflare Pages** (работает в России)
- **Vercel** (может быть заблокирован)
- **Netlify** (может быть заблокирован)

