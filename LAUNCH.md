# 🚀 Запуск Mini App с вашим токеном

Токен бота уже сохранен в `backend/.env`

## Шаг 1: Установите зависимости (если еще не установлены)

```bash
cd /Users/andreeviich/telegram-planner-app
npm run install:all
```

## Шаг 2: Создайте Mini App в BotFather

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newapp`
3. Выберите вашего бота из списка
4. Заполните информацию:
   - **Title**: Планировщик задач
   - **Short name**: planner (или любое другое)
   - **Description**: Планировщик задач и дневник
   - **Photo**: (опционально)
   - **Web App URL**: **пока оставьте пустым** - заполним после запуска ngrok

## Шаг 3: Запустите приложение

### Вариант A: Автоматический запуск

```bash
cd /Users/andreeviich/telegram-planner-app
./start-dev.sh
```

### Вариант B: Ручной запуск (3 терминала)

**Терминал 1 - Backend:**
```bash
cd /Users/andreeviich/telegram-planner-app/backend
npm run dev
```
Должно появиться: `Server running on port 3001`

**Терминал 2 - Frontend:**
```bash
cd /Users/andreeviich/telegram-planner-app/frontend
npm run dev
```
Должно появиться: `Local: http://localhost:3000/`

**Терминал 3 - ngrok (если не установлен: `brew install ngrok`):**
```bash
ngrok http 3000
```

## Шаг 4: Получите HTTPS URL от ngrok

1. Откройте в браузере: http://localhost:4040
2. Найдите строку **Forwarding**: `https://xxxx-xx-xx-xx-xx.ngrok-free.app -> http://localhost:3000`
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok-free.app`)

## Шаг 5: Подключите Mini App к боту

1. В Telegram откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/myapps`
3. Выберите ваше приложение из списка
4. Выберите **"Edit"** → **"Web App URL"**
5. Вставьте HTTPS URL от ngrok (например: `https://abc123.ngrok-free.app`)
6. Подтвердите

## Шаг 6: Откройте Mini App в Telegram

1. Найдите вашего бота в Telegram
2. Нажмите на кнопку меню (три полоски) или отправьте `/start`
3. Вы увидите кнопку **"Open App"** или **"Launch App"**
4. Нажмите на неё - откроется ваше Mini App! 🎉

## Проверка работы

После открытия приложения:
- ✅ Должна открыться страница с планировщиком задач
- ✅ Кнопка "+" должна создавать новые задачи
- ✅ Вкладка "Календарь" должна показывать календарь
- ✅ Все должно работать в темной теме с glass эффектом

## Если что-то не работает

### Приложение не открывается
- Проверьте, что ngrok запущен и показывает HTTPS URL
- Убедитесь, что URL правильно вставлен в BotFather
- Проверьте консоль браузера (в Telegram: долгое нажатие на приложение → "Inspect Element")

### Backend не запускается
- Проверьте, что порт 3001 свободен: `lsof -i :3001`
- Убедитесь, что установлены зависимости: `cd backend && npm install`

### Frontend не запускается
- Проверьте, что порт 3000 свободен: `lsof -i :3000`
- Убедитесь, что установлены зависимости: `cd frontend && npm install`

### API запросы не работают
- Проверьте `frontend/.env` - должен быть `VITE_API_URL=http://localhost:3001/api`
- Убедитесь, что backend запущен
- Проверьте консоль браузера на ошибки CORS

## Полезные команды

```bash
# Остановить все процессы на портах
lsof -ti:3000 | xargs kill -9
lsof -ti:3001 | xargs kill -9

# Проверить, что работает
curl http://localhost:3001/health
curl http://localhost:3000
```

## Следующие шаги

После успешного запуска локально, для продакшена:
1. Задеплойте frontend на Vercel
2. Задеплойте backend на Railway
3. Обновите Web App URL в BotFather на продакшен URL

