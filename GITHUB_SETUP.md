# 📤 Как загрузить код на GitHub - Пошаговая инструкция

## Шаг 1: Создайте аккаунт на GitHub (если еще нет)

1. Откройте [github.com](https://github.com)
2. Нажмите **Sign up** (Регистрация)
3. Заполните форму и создайте аккаунт
4. Подтвердите email

---

## Шаг 2: Создайте новый репозиторий на GitHub

1. Войдите в свой аккаунт на GitHub
2. Нажмите на **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Заполните форму:
   - **Repository name**: `telegram-planner-app` (или любое другое имя)
   - **Description**: "Telegram Mini App - Планировщик задач"
   - **Visibility**: выберите **Public** (или Private, если хотите приватный)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть файлы)
5. Нажмите **"Create repository"**

---

## Шаг 3: Скопируйте команды от GitHub

После создания репозитория GitHub покажет страницу с инструкциями. 

**Важно:** Выберите вариант **"…or push an existing repository from the command line"**

Вы увидите команды типа:
```bash
git remote add origin https://github.com/ваш-username/telegram-planner-app.git
git branch -M main
git push -u origin main
```

---

## Шаг 4: Выполните команды в терминале

Откройте **Terminal** (на Mac: Applications → Utilities → Terminal)

Выполните команды по порядку:

### 4.1. Перейдите в папку проекта:
```bash
cd /Users/andreeviich/telegram-planner-app
```

### 4.2. Проверьте статус:
```bash
git status
```

### 4.3. Добавьте все файлы (если еще не добавлены):
```bash
git add .
```

### 4.4. Создайте коммит:
```bash
git commit -m "Initial commit - Telegram Planner App"
```

### 4.5. Подключите GitHub репозиторий:
```bash
git remote add origin https://github.com/ВАШ-USERNAME/telegram-planner-app.git
```
**⚠️ Замените `ВАШ-USERNAME` на ваш реальный username с GitHub!**

Например, если ваш username `john`, команда будет:
```bash
git remote add origin https://github.com/john/telegram-planner-app.git
```

### 4.6. Переименуйте ветку в main (если нужно):
```bash
git branch -M main
```

### 4.7. Загрузите код на GitHub:
```bash
git push -u origin main
```

**При первом push GitHub попросит авторизоваться:**
- Введите ваш GitHub username
- Введите пароль (или Personal Access Token, если включена 2FA)

---

## Шаг 5: Проверьте результат

1. Откройте ваш репозиторий на GitHub: `https://github.com/ваш-username/telegram-planner-app`
2. Вы должны увидеть все файлы проекта
3. Готово! 🎉

---

## Если возникли проблемы:

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ваш-username/telegram-planner-app.git
```

### Ошибка авторизации
GitHub больше не принимает пароли. Нужно создать **Personal Access Token**:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и права (достаточно `repo`)
4. Скопируйте токен
5. Используйте токен вместо пароля при `git push`

### Не знаете свой GitHub username?
- Откройте github.com
- Нажмите на аватар в правом верхнем углу
- Ваш username будет внизу меню

---

## Визуальная инструкция (альтернатива)

Если не хотите использовать терминал, можно использовать **GitHub Desktop**:

1. Скачайте [GitHub Desktop](https://desktop.github.com)
2. Войдите в аккаунт
3. File → Add Local Repository
4. Выберите папку `/Users/andreeviich/telegram-planner-app`
5. Нажмите "Publish repository"
6. Выберите имя и нажмите "Publish"

---

## Что дальше?

После загрузки кода на GitHub:
1. ✅ Код на GitHub
2. 📦 Деплой Backend на Railway (см. QUICK_DEPLOY.md)
3. 🎨 Деплой Frontend на Vercel (см. QUICK_DEPLOY.md)
4. 🤖 Подключение к Telegram

---

## Нужна помощь?

Если что-то не получается, напишите:
- Какую ошибку видите
- На каком шаге остановились

