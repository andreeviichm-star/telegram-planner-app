# 📝 Создание workflow через веб-интерфейс GitHub

## Проблема
Git не может загрузить workflow файл без прав `workflow` в токене.

## Решение: Создать через веб-интерфейс

---

## Шаг 1: Откройте репозиторий

1. Зайдите на: `https://github.com/andreeviichm-star/telegram-planner-app`
2. Перейдите в папку `.github/workflows/`
3. Если папки нет - создайте её:
   - Нажмите **Add file** → **Create new file**
   - В пути введите: `.github/workflows/deploy.yml`
   - GitHub автоматически создаст папки

---

## Шаг 2: Создайте файл deploy.yml

1. Если файла нет:
   - Нажмите **Add file** → **Create new file**
   - В пути введите: `.github/workflows/deploy.yml`

2. Если файл уже есть:
   - Нажмите на файл `deploy.yml`
   - Нажмите на карандаш (✏️) для редактирования

---

## Шаг 3: Вставьте содержимое

Скопируйте и вставьте это содержимое:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Build
        run: |
          cd frontend
          npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_BASE_PATH: /telegram-planner-app/
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend/dist
          cname: false
```

---

## Шаг 4: Сохраните файл

1. Прокрутите вниз страницы
2. В поле "Commit new file" или "Commit changes":
   - Введите сообщение: `Add GitHub Pages deployment workflow`
3. Нажмите **"Commit new file"** или **"Commit changes"** (зеленая кнопка)

---

## Шаг 5: Проверьте, что файл создан

1. Перейдите в `.github/workflows/`
2. Должен быть файл `deploy.yml`
3. Откройте его - содержимое должно совпадать с тем, что вы вставили

---

## Шаг 6: Запустите workflow

1. Репозиторий → **Actions** (вверху)
2. В левом меню должен появиться **"Deploy to GitHub Pages"**
3. Если его нет - обновите страницу (F5)
4. Нажмите на workflow
5. Нажмите **"Run workflow"** (справа вверху)
6. Выберите ветку: **main**
7. Нажмите **"Run workflow"**

---

## Шаг 7: Дождитесь завершения

1. В **Actions** дождитесь завершения workflow
2. Должен быть зеленый статус ✅
3. После этого сайт должен обновиться

---

## Готово! 🎉

После выполнения всех шагов:
- ✅ Workflow создан на GitHub
- ✅ Workflow отображается в Actions
- ✅ Можно запускать вручную или автоматически при push

---

## Альтернатива: Обновить токен

Если хотите загружать workflow через git:

1. GitHub → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. Создайте новый токен или отредактируйте существующий
3. Добавьте галочку **workflow**
4. Сохраните токен
5. Используйте новый токен для git

Но проще создать через веб-интерфейс! 😊

